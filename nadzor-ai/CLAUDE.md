# ФИНАЛЬНЫЙ ПРОМТ ДЛЯ CLAUDE CODE
## «НАДЗОР.ИИ» — модуль предиктивного строительного надзора

> Версия 2.1. Заменяет предыдущие редакции. Сохранить как `CLAUDE.md` в корне репозитория.
> Задача города № 10, Комитет государственного строительного надзора города Москвы.

> **СНАЧАЛА `CONTEXT.md` (соседний файл), потом этот.** Приложение Г — это
> 88 пунктов журнала находок, накопленных за много прогонов; к любой одной
> задаче относится примерно треть из них, остальные — про этапы, до
> которых сейчас очередь не дошла. `CONTEXT.md` в двух страницах говорит,
> на каком мы этапе, какие пункты Г.N применимы сейчас, а какие открывать
> не нужно. Читать Приложение Г целиком «на всякий случай» — тот самый
> способ потерять день, которым он уже был потерян (см. Г.76→Г.78).

> **Как читать этот файл.** Разделы 0–9 и Приложения Б/В — исходное
> техзадание полной CRM. Её прототип (`packages/api`+`analysis`+
> `documents`+`llm_core`+`norms`+`integrations`+`audit`) **удалён из
> репозитория целиком, см. Г.85** — постановка задачи осталась, кода
> больше нет. Приложение Г — механика единственного оставшегося движка
> (`packages/backend`, порт 8010), которым реально ведётся разбор ПД и
> сравнение документов. Приложение Г сжато до
> справочных правил; полный разбор с доказательствами и историей прогонов
> для каждого Г.N — `docs/PRILOZHENIE-G-ISTORIYA.md`.
>
> **Правило при добавлении новой записи Г.N (иначе файл снова раздуется):**
> сюда — короткое правило на 3-4 строки (что было не так → что сделано →
> ключевая цифра/граница), полный текст с доказательствами — сразу в
> `docs/PRILOZHENIE-G-ISTORIYA.md`. Ровно это правило перестали соблюдать
> на Г.50–Г.83: записи писались длинными прямо здесь (13.9 строки против
> 4.4 у ранних), файл истории не пополнялся, Приложение Г выросло до 470
> строк. Возвращено на место в той же сессии — 257 строк, ни одна находка
> не потеряна.

---

## 0.0 ИСХОДНАЯ ФОРМУЛИРОВКА ЗАДАЧИ — ИСТОЧНИК ИСТИНЫ

Дословный текст задачи организатора. При расхождении любого раздела этого
документа с формулировкой ниже — побеждает формулировка ниже.

> Разработать ИИ-сервис, который будет автоматически сравнивать изменения
> между этапами проектирования, анализировать исполнительную документацию и
> помогать инспекторам находить потенциальные нарушения ещё до проверки
> объекта.
>
> Каждая стройка сопровождается тысячами страниц документации, и их ручная
> проверка требует много времени. Интеллектуальный сервис позволит быстрее
> выявлять несоответствия, повысит эффективность строительного контроля и
> поможет инспекторам приходить на объект уже с готовым анализом возможных
> нарушений. Лучшее решение планируется внедрить в существующие системы
> строительного надзора, а в перспективе — масштабировать по всей стране.

### Три обязательных результата и где они живут

| Что требует задача | Чем закрыто | Признак, что это действительно работает |
|---|---|---|
| Сравнивать изменения между этапами проектирования | Переходы T1, T3, T4 (раздел 1); подбор пар листов и раздельные пулы «чертёж/текст» | Комплекты разного объёма сравниваются целиком: ни один лист ни с одной стороны не выпадает из разбора |
| Анализировать исполнительную документацию | Переходы T5, T6; текстовое сравнение актов, ведомостей и спецификаций | Находится объём или класс материала ниже проектного и нарушенная хронология освидетельствования скрытых работ |
| Находить потенциальные нарушения **до** выезда на объект | `severity` + `field_check` в каждой находке; карта внимания | Инспектор получает не список различий, а порядок обхода и конкретное действие на месте |

### Что из этого следует для формы вывода

«Тысячи страниц» и «прийти с готовым анализом» задают жёсткое требование к
находке: она обязана отвечать **куда идти и что там проверить**, а не «чем
отличаются два листа».

- **`severity`** (`critical` / `major` / `minor`) задаёт порядок обхода
  объекта. Список, отсортированный по времени разбора, а не по критичности,
  задачу не решает.
- **`field_check`** — одно действие на месте: что измерить, вскрыть, сверить
  или какой документ истребовать.
- **Координатный ориентир на листе** (оси, номер помещения, зона) обязателен:
  расхождение без места на листе инспектору бесполезно.
- Экономия времени — измеримый результат задачи, поэтому объём разобранного и
  доля отсеянного шума показываются пользователю, а не остаются в логах.

Ограничение честности: локальная модель **не** цитирует номера пунктов СП и
ГОСТ — 7B-модель их выдумывает, а выдуманный реквизит хуже отсутствующего
(раздел 0, правило 7). Нормативные ссылки даёт только слой `norms` с поиском
по реальной базе.

### Что следует из «внедрить в существующие системы» и «масштабировать по стране»

Это не пункт презентации, а требование к архитектуре, уже зафиксированное в
разделах 2 (адаптеры СУДИР / ИАИС ОГД), 6 (модульный монолит с границами
будущих сервисов) и В.5 (регион как конфигурация). Решение оценивается как
встраиваемый модуль: собственный контур идентификации, собственный источник
данных об объектах и жёстко зашитая нормативная база региона — дефекты, а не
упрощения.

---

## 0. РОЛЬ, РЕЖИМ, ЖЁСТКИЕ ПРАВИЛА

Ты — ведущий архитектор и разработчик системы государственного назначения. Мы создаём **модуль, встраиваемый в существующую цифровую экосистему стройкомплекса Москвы** (референс — платформа ЛОКИ, ГБУ ЦЭИИС), а не изолированный продукт.

**Правила:**
1. Сначала выведи план и структуру каталогов, дождись подтверждения, потом пиши код.
2. Все внешние зависимости (LLM, OCR, СУДИР, ИАИС ОГД, ЭДО) — только через абстрактные интерфейсы Port/Adapter. Прямых вызовов SDK в бизнес-логике нет.
3. **Каждый вывод о нарушении обязан содержать ссылку на пункт нормативного документа и координаты на листе исходного документа.** Вывод без источника — критический дефект, покрой это тестом.
4. Модули общаются только через публичные интерфейсы. Добавь `import-linter` в CI, проверяющий границы.
5. Комментарии и документация — на русском. Код и имена — на английском.
6. Пиши так, будто код будут читать на аудите ФСТЭК.
7. Не выдумывай реквизиты внешних API. Где спецификация неизвестна — делай адаптер с честным моком и файлом `docs/integration-questions.md` со списком вопросов к владельцу системы.
8. **ВСЁ ОБЩЕЕ ДЛЯ ЛЮБОЙ ДОКУМЕНТАЦИИ. Пример, на котором ты сейчас работаешь, не попадает ни в код, ни в комментарии, ни в промпты, НИ В ЭТУ ИНСТРУКЦИЮ.**
   Правило нарушалось четырежды (Г.88, Г.89, Г.102, Г.104), каждый раз незаметно для самого автора, и каждый раз его находил пользователь. Поэтому оно здесь, среди жёстких правил, а не в приложении, и поэтому у него есть машинная проверка.
   - **Инструкция — не исключение, а самый опасный случай.** Код можно прочитать и понять; инструкция задаёт способ думать ДО того, как открыт первый файл. Правило, изложенное словами одного раздела, читается как правило про этот раздел, даже когда механизм общий.
   - Наблюдение о конкретном разделе или документе — это ДАННЫЕ. Их место в реестре (`table_registry`, `subsystem`, `set_overview`, `known_violations.json`), где у записи есть честный статус n=1/n=0. Подключение нового раздела — заполнение реестра, а не правка кода.
   - Механика к разделу не обращается: ни сравнением кода раздела со строковой константой, ни лексикой одной дисциплины в регулярке, ни примером из одного тома в докстринге. Комментарий задаёт рамку следующему читателю не слабее кода, а попав в промпт — якорит модель на чужом документе.
   - Реквизиты проверяемого объекта (шифр, организация, маркировка тома, его текст) в репозиторий не идут вообще: это утечка эталона следующей «слепой» сессии (Г.24) и данных объекта в git (Г.12). Форму объясняй обезличенным шаблоном («XXX/000000/1-РД-<марка>»).
   - Не уверен, обобщать или зашить: **зашивать нельзя**. Если правило выведено из одного комплекта — это запись реестра со статусом n=1, а не строчка в механике.
   - **Что запрещать НЕЛЬЗЯ: явную пометку границы.** «Это раздело-специфично», «статус n=1», «на втором комплекте не проверено» — не конкретика, а признание области применимости. Убрать такую пометку значит выдать узкий механизм за общий — то самое, что Г.42 назвал настоящим overfitting. Обобщать надо формулировку правила, а не прятать его границы.
   - **Где конкретика уместна: в журнале доказательств** (`docs/PRILOZHENIE-G-ISTORIYA.md`). «На таком-то томе измерено то-то» — это запись наблюдения, и вычищать из неё предмет замера значит превращать доказательство в лозунг. Реквизиты объекта — шифр, организация, маркировка, имя файла — запрещены и там: доказательности они не добавляют, но это данные объекта в git и готовый ответ следующей «слепой» сессии.
   - Перед коммитом: `make check-generic`. Падение означает не «поправь строчку», а «наблюдение попало туда, где ему не место».

   **ПОРЯДОК ЛЮБОЙ ПРАВКИ (обязателен, проверяется машиной).** Правило нарушалось десять раз подряд, каждый раз незаметно для автора и каждый раз его находил пользователь. Словесных формулировок было достаточно ещё на Г.88 — не хватало процедуры. Она такая:
   1. **Прежде чем писать число — спроси, из чего оно берётся.** Любая новая числовая константа обязана быть объявлена в `test_constants_are_declared.py` одним из видов: БЮДЖЕТ (сколько работы делать), ПРАВИЛО ЯЗЫКА, ГЕОМЕТРИЯ/ФОРМАТ, ПОДОГНАНО с честным `n=` (на скольких документах измерено). Незаявленная константа роняет `make check-generic`. Подгонка не запрещена — запрещена НЕЗАМЕТНАЯ подгонка.
   2. **Порог, выведенный из одного комплекта, — не правило, а наблюдение.** Если значение подобрано по тому, что сейчас на столе, у него два законных места: вычисляемая величина (считается по разбираемому документу) или накопительная база (считается по числу РАЗНЫХ томов). Число в коде — третье место, и оно требует явной пометки `ПОДОГНАНО, n=…`.
   3. **Не отсеивай там, где можно ранжировать.** Отсев требует порога, порядок — нет. Сколько взять из порядка, решает бюджет потребителя, а не придуманная граница истины.
   4. **Проверяй правку на документе, которого не видел.** Если правку нельзя объяснить, не называя разобранный комплект, — это подгонка. Формулируй правило так, чтобы оно читалось без единого слова о текущем объекте: роль вместо отраслевого имени, свойство формы вместо конкретного листа.
   5. **Отрицательный результат измерения — тоже результат.** Не нашлось границы, отделяющей нужное от шума, — так и записывай, а не выбирай число «примерно посередине». Прятать отсутствие правила за правдоподобной константой хуже, чем оставить видимый шум.
   6. **Спрашивай себя вопросом пользователя:** «если бы сюда положили сто томов, эта правка работала бы?» Если ответ «нет, пришлось бы подстроить» — механизм должен брать знание из накопительной базы, а не из головы автора.

---

## 1. ЦЕНТРАЛЬНАЯ ИДЕЯ ПРОДУКТА

Инспектор загружает комплект документации (или он приходит из внешней системы), и получает ответ на один вопрос: **на что смотреть в первую очередь и почему**.

Ключевая абстракция — **переход между состояниями объекта**. Система сравнивает не «проект с проектом», а любые два состояния жизненного цикла:

| # | Переход | Что ищем |
|---|---|---|
| T1 | ПД ред. N → ПД ред. N+1 | изменения, требующие повторной экспертизы (ст. 49 ГрК РФ) |
| T2 | ПД → заключение экспертизы | учтены ли замечания экспертизы |
| T3 | ПД → РД | отступления рабочей документации от проектных решений |
| T4 | РД ред. N → РД ред. N+1 | изменения в производстве работ |
| T5 | РД → ИД | соответствие фактически выполненного проекту |
| T6 | ИД → ИД | внутренняя непротиворечивость комплекта (даты, объёмы, комплектность) |
| T7 | любое состояние → нормативная база | соответствие СП, ГОСТ, техрегламентам |

**Один движок, семь переходов.** Реализуй как `TransitionAnalyzer` с общим контрактом: на вход два `DocumentSet`, на выходе список `Finding`. Тип перехода определяет набор применяемых правил и промптов, но не архитектуру.

Это покрывает и стадию проектирования, и стадию производства работ единым механизмом.

---

## 2. ИНТЕГРАЦИИ: РАЗДЕЛЕНИЕ КОНТУРОВ

Критически важно не путать назначение внешних систем.

### 2.1 СУДИР — контур идентификации и доступа
Единая система управления доступом к информационным ресурсам города Москвы. Из неё приходит **личность и полномочия**, а не данные об объектах.

Реализуй `IdentityProvider`:
- аутентификация по протоколу единого входа (OAuth 2.0 / OpenID Connect либо SAML 2.0 — уточнить у владельца системы, реализовать через конфигурируемый адаптер);
- получение атрибутов сотрудника: ФИО, СНИЛС-идентификатор, подразделение, должность, срок действия учётной записи;
- получение ролей и полномочий → маппинг на внутреннюю RBAC-модель;
- единый выход (single logout), корректная обработка отзыва прав.

**Внутренние роли (не изобретай свои, маппь на присланные):**
| Роль | Права |
|---|---|
| Инспектор | свои объекты, оценка гипотез, формирование проекта акта |
| Начальник отдела | объекты отдела, переназначение, разблокировка оценок |
| Эксперт ЦЭИИС | лабораторные данные, испытания, заключения |
| Аналитик | сводная аналитика без доступа к персональным данным |
| Администратор | конфигурация моделей, правил, интеграций |
| Аудитор | только чтение журнала аудита, без доступа к содержимому |

Для разработки — `MockIdentityProvider` с переключением роли в один клик (нужно для демо: показать, как один и тот же объект выглядит для инспектора и для начальника отдела).

### 2.2 ИАИС ОГД — контур градостроительных данных
Информационно-аналитическая система обеспечения градостроительной деятельности. Отсюда приходят **сведения об объекте**, избавляющие инспектора от ручного ввода.

Реализуй `UrbanDataProvider`:
- карточка объекта капитального строительства по номеру разрешения на строительство;
- разрешение на строительство и его изменения, ГПЗУ, сведения о земельном участке;
- реквизиты и даты заключений государственной экспертизы (сверять с ЕГРЗ);
- сведения о застройщике, техническом заказчике, лице, осуществляющем строительство, генпроектировщике; членство в СРО;
- этапность строительства, плановые сроки, сведения о ранее выданных предписаниях;
- история проверок объекта.

**Сценарий использования:** инспектор вводит номер разрешения → карточка объекта заполняется автоматически → остаётся только приложить документацию. Ручное создание объекта возможно, но помечается как «данные не подтверждены ИАИС ОГД».

### 2.3 Прочие адаптеры (интерфейсы + моки)
| Система | Назначение |
|---|---|
| ЕГРЗ | проверка наличия и актуальности заключений экспертизы |
| АИС «Стройнадзор» | выгрузка проектов актов и предписаний |
| ЛОКИ / ЦЭИИС | результаты лабораторных испытаний, привязка к конструктивам |
| СОД застройщика (Exon, Pilot-BIM и аналоги) | автоматическое получение ИД |
| ЭДО (Диадок, СБИС) | подписанные документы |
| 1С:Предприятие | OData v4 — сметы, объёмы, материалы |
| Реестр СРО (НОСТРОЙ, НОПРИЗ) | подтверждение допусков |
| ФГИС ЦС | нормативы расхода ресурсов для проверки правдоподобности объёмов |

Каждый адаптер — отдельный пакет, contract-тесты, версионирование схем.

---

## 3. АБСТРАКЦИЯ LLM (не менять)

Пакет `packages/llm_core`:

```python
class LLMProvider(Protocol):
    name: str
    supports_function_calling: bool
    max_context_tokens: int
    is_sovereign: bool          # размещён в РФ / реестр отечественного ПО

    async def complete(self, req: CompletionRequest) -> CompletionResponse: ...
    async def embed(self, texts: list[str]) -> list[Vector]: ...
    async def health(self) -> ProviderHealth: ...
```

Реализации: `GigaChatProvider` (продуктив), `YandexGPTProvider` (продуктив), `LocalVLLMProvider` (целевой, изолированный контур), `ClaudeProvider` (только demo/разработка), `MockProvider` (тесты и офлайн-демо).

`ProviderRouter` выбирает адаптер по политике безопасности объекта, длине контекста, доступности и бюджету. Конфигурация — `config/providers.yaml`, без пересборки.

**Демо-требование:** переключение провайдера на GigaChat одной строкой конфига вживую перед жюри.

---

## 4. ИНТЕРФЕЙС — ГЛАВНЫЙ ПРИОРИТЕТ

Визуальный язык наследуется от платформы ЛОКИ: левое сворачиваемое двухуровневое меню, глобальный поиск в шапке, белые карточки со сворачиваемыми секциями на светло-сером фоне, сортируемые таблицы-журналы, обязательные поля со звёздочкой, подсказки с иконкой ⓘ. Акцентный цвет — мягкий сине-фиолетовый; токены вынести в один файл `theme.ts` для быстрой замены по официальному гайдлайну.

Разделы меню модуля (в терминологии платформы):
```
Предиктивный надзор
 ├── Журнал анализа документации
 ├── Журнал расхождений
 ├── Журнал гипотез нарушений
 ├── Карта внимания
 └── Настройки моделей и правил
```

### 4.1 Загрузка документации — центральный сценарий
Экран `Новый анализ`:
1. Поле «Номер разрешения на строительство» → автозаполнение карточки объекта из ИАИС ОГД, показ подтянутых данных с пометкой источника.
2. Зона перетаскивания файлов с явным указанием **типа состояния**: «Проектная документация», «Рабочая документация», «Заключение экспертизы», «Исполнительная документация». Инспектор кидает папку — система сама раскладывает по типам и просит подтвердить спорные.
3. Автоопределение редакции и даты из штампа (основная надпись по ГОСТ Р 21.1101) с возможностью правки.
4. Выбор переходов для анализа: чекбоксы T1–T7, по умолчанию отмечены доступные по составу загруженного.
5. Прогресс по этапам с честными формулировками: распознавание → извлечение сущностей → сравнение → сверка с нормативами → перепроверка → готово. Промежуточные находки показывать по мере готовности, не держать пустой экран.

### 4.2 «Карта внимания» — сигнатурный экран
Не таблица. Компактный список из 5–7 позиций, отвечающий на вопрос «куда идти на объекте»:
- конструктивный элемент и его расположение (оси, отметка, секция);
- одна строка сути расхождения;
- что конкретно проверить или измерить на месте;
- какие документы взять с собой / истребовать;
- отметка «проверено» прямо с мобильного, офлайн.

Печать и выгрузка в PDF — инспектор должен иметь возможность распечатать её и взять на объект.

### 4.3 Журнал расхождений
Таблица переходов с фильтрами по типу перехода, разделу проекта, критичности. Для каждого расхождения — двухпанельный просмотр «было / стало» с подсветкой изменённой области на листе. Для текстовых различий — построчный diff, для графики — наложение с выделением зон.

### 4.4 Журнал гипотез нарушений
Реализован в макете `nadzor-ai-module.jsx`. Карточка гипотезы: общая информация, доказательная база с переходом к листу документа, нормативное и правовое основание, рекомендуемое действие, оценка инспектора (подтверждено / не подтверждено / уточнить).

### 4.5 Мобильный режим
PWA, offline-first, IndexedDB, фоновая синхронизация. На объекте инспектор открывает карту внимания, отмечает проверенное, делает фотофиксацию с привязкой к конкретной гипотезе и конструктиву. Связь на стройке нестабильна — офлайн не опция, а требование.

### 4.6 Требования к качеству интерфейса
Адаптивность до мобильного, видимый фокус клавиатуры, соблюдение `prefers-reduced-motion`, контраст не ниже AA, осмысленные пустые состояния и ошибки («что произошло и что сделать», без извинений и без технических кодов в лицо пользователю).

---

## 5. МОДУЛИ

| Пакет | Ответственность |
|---|---|
| `identity` | СУДИР-адаптер, RBAC, сессии |
| `urban_data` | ИАИС ОГД, ЕГРЗ, СРО |
| `ingest` | приём, валидация ЭЦП (ГОСТ Р 34.10-2012), дедупликация, версионирование |
| `parsing` | OCR, разбор штампа, извлечение сущностей в строгую JSON-схему с `source_ref` = {file_id, page, bbox} |
| `lifecycle` | модель состояний объекта, `DocumentSet`, определение доступных переходов |
| `diffing` | `DiffEngine`: растровый/PDF, DXF, IFC (интерфейс сейчас, реализация в фазе 2) |
| `norms` | нормативная база, чанкинг **по пунктам**, гибридный поиск, контроль актуальности редакций |
| `detection` | слой детерминированных правил + LLM-слой + верификационный проход другой моделью |
| `scoring` | приоритизация: критичность конструктива × тяжесть по КоАП × уверенность × стадия готовности × история подрядчика |
| `attention` | формирование карты внимания из гипотез |
| `reporting` | проект акта проверки, предписание, выгрузка |
| `audit` | неизменяемый журнал: кто, когда, какая модель, версия промпта, хэш модели, вердикт |
| `integrations` | внешние адаптеры |

### 5.1 Детектор: гибридная схема
- **Правила** (детерминированные, воспроизводимые): хронология дат, комплектность, обязательные реквизиты, арифметика объёмов, действительность допусков СРО. Покрывают формальные нарушения на 100%.
- **LLM** (семантика): сопоставление проект ↔ ИД ↔ норматив. Работает только с извлечёнными фактами и цитатами из RAG, свободная генерация запрещена промптом.
- **Верификация**: второй проход другой моделью или другим провайдером. Расхождение → понижение уверенности и флаг «требует внимания эксперта».

### 5.2 Контракт находки
```json
{
  "id": "H-1042",
  "transition": "T5",
  "code": "D-02",
  "severity": "critical|major|minor",
  "confidence": 0.93,
  "title": "Класс материала в исполнительной документации ниже проектного",
  "structural_element": {
    "name": "<наименование конструктива или системы>",
    "axes": "<оси>",
    "level": "<отметка>",
    "section": "<секция или этаж>"
  },
  "evidence": [
    {"doc_id": "...", "page": 14, "bbox": [x0,y0,x1,y1], "extracted": "<значение по проекту>", "role": "требование"},
    {"doc_id": "...", "page": 1,  "bbox": [x0,y0,x1,y1], "extracted": "<значение по факту>", "role": "факт"}
  ],
  "norm_refs": [{"doc": "<обозначение норматива>", "clause": "<пункт>", "edition": "<год>", "actual": true}],
  "legal_refs": [{"act": "КоАП РФ", "article": "9.4", "part": "2"}],
  "field_check": "<одно действие на месте: что измерить, вскрыть или сверить, и где именно>",
  "documents_to_request": ["<протокол испытаний>", "<журнал производства работ за период>"],
  "verification": {"status": "confirmed", "provider": "yandexgpt", "agreement": true},
  "provenance": {"model": "GigaChat-Pro", "prompt_version": "2.3", "run_id": "...", "created_at": "..."}
}
```

---

## 6. СТЕК И РАЗВЁРТЫВАНИЕ

**Backend:** Python 3.12, FastAPI, Pydantic v2, SQLAlchemy 2, Alembic
**Данные:** PostgreSQL 16 + pgvector, S3-совместимое хранилище (интерфейс, локальная ФС в демо)
**Обработка:** PyMuPDF, OpenCV, PaddleOCR, `ezdxf`, `ifcopenshell` (фаза 2), sentence-transformers
**Frontend:** React 18 + TypeScript, Vite, TanStack Query, Zustand, Tailwind, PWA (Workbox)
**Инфраструктура:** Docker Compose (демо) → Kubernetes (продуктив), Astra Linux SE как целевая ОС
**Наблюдаемость:** OpenTelemetry, Prometheus, structlog

**Топология фазы 1 — модульный монолит** с жёсткими границами пакетов. В `docs/architecture.md` явно обосновать: границы модулей соответствуют будущим сервисам (диаграмма C4), извлечение любого модуля не требует изменения бизнес-логики.

**Размещение:** защищённое облако города, доступ через браузер без установки на рабочие места. Формулировка для презентации — «веб-приложение в защищённом облаке города», не «облачный сервис»: данные не покидают контур, требования КИИ и 152-ФЗ соблюдены.

---

## 7. ДАННЫЕ ДЛЯ ДЕМО

Реальную документацию использовать нельзя. Датасет строится **первым, до кода**, и определяет схемы, промпты и метрики.

**Слой А — отладочный:** синтетический комплект по монолитному жилому дому: ПД ред. 2 и ред. 3, РД, 12 АОСР по формам РД-11-02-2006, общий журнал работ, 4 паспорта качества, 2 протокола испытаний. 6 изменений между редакциями + 5 нарушений в ИД. Эталон — `ground_truth.json`.

**Слой Б — валидационный:** таксономия типовых нарушений, собранная из публичных источников (постановления Мосгосстройнадзора о привлечении к ответственности, картотека арбитражных дел по ст. 9.4 и 9.5 КоАП, ежегодные доклады о правоприменительной практике). Собирается **после** написания детектора, служит независимой проверкой. Файл `data/violations_taxonomy.json` с частотностью.

Достоверность обязательна: корректные формы, реальная терминология, правдоподобные объёмы и сроки. Документы должны выглядеть так, чтобы действующий инспектор не нашёл в них фальши.

**Скрипт `scripts/evaluate.py`** — precision, recall, F1 по каждому классу расхождений отдельно. Метрики на слайде превращают демо в измеримый результат.

**Демо-объекты по умолчанию выключены.** Система стартует с пустым списком:
в журналах и на дашборде только то, что загрузил пользователь. Витрина с
вымышленными объектами включается `SEED_DEMO_OBJECTS=1` — для показа, не для
работы. Сотрудники (контур СУДИР) заводятся всегда: без них ролям в
интерфейсе не с чем сопоставляться.

### `data/known_violations.json` — известные нарушения в промпт

Реальные нарушения из практики надзора подставляются в системный промпт
сравнения листов (`packages/backend/app/vision.py`), отфильтрованные по типу
листа (чертёж/текст) и коду раздела. Это **не** дообучение весов: работает с
любым провайдером, включая локальный Ollama, и правится без пересборки.

Пополнять — добавляя запись в `examples` (формат описан внутри файла). Сюда
идут только реально встречавшиеся нарушения: выдуманный пример научит модель
выдумывать (раздел 0, правило 7). Отсутствие или порча файла не роняет
анализ — промпт просто остаётся общим.

**Источник записи — обязательно другой объект, не тот, что сейчас проверяется
вслепую (Г.12, Г.11).** Иначе получается не обобщение практики, а переобучение
на один эталон под видом общего правила — и заодно утечка эталона в промпт
работающей системы. Раздел-специфичные примеры (`discipline != "*"`) заводятся
только когда есть независимо разобранный второй комплект той же дисциплины;
до тех пор в файле — только универсальные (`discipline: "*"`) категории.

---

## 8. КРИТЕРИИ ГОТОВНОСТИ

Первые три пункта — дословные результаты из формулировки задачи (раздел 0.0).
Пока не закрыт любой из них, остальное значения не имеет.

- [ ] Сравнение изменений между этапами проектирования идёт по всему объёму:
      комплекты разной толщины разбираются целиком, ни один лист не выпадает
- [ ] Исполнительная документация анализируется по существу: занижение
      объёма или класса материала и нарушенная хронология освидетельствования
      скрытых работ попадают в находки
- [ ] Каждая находка даёт инспектору критичность, место на листе и конкретное
      действие на объекте — до выезда, а не после
- [ ] `docker compose up` поднимает всё одной командой
- [ ] Сценарий «загрузил ПД и РД → получил карту внимания» проходит за ≤ 5 минут
- [ ] Каждая гипотеза кликабельно ведёт к области на листе документа
- [ ] Каждая гипотеза содержит пункт норматива с проверкой актуальности редакции
- [ ] Вход через мок СУДИР с переключением роли; данные объекта из мока ИАИС ОГД
- [ ] Переключение LLM-провайдера одной строкой конфига продемонстрировано
- [ ] Карта внимания печатается и работает офлайн на мобильном
- [ ] `evaluate.py` выдаёт метрики по `ground_truth.json`
- [ ] Покрытие тестами ядра ≥ 70%, `import-linter` в CI зелёный
- [ ] `docs/`: архитектура C4, модель угроз, импортозамещение, roadmap с трудозатратами, OpenAPI, `integration-questions.md`

---

## 9. ЧТО СДЕЛАТЬ ПЕРВЫМ

Выведи и остановись для подтверждения:
1. Дерево каталогов монорепозитория.
2. DDL основных таблиц (объекты, состояния, документы, находки, оценки, аудит).
3. Определения интерфейсов: `LLMProvider`, `DiffEngine`, `TransitionAnalyzer`, `IdentityProvider`, `UrbanDataProvider`, `IntegrationAdapter`.
4. `docker-compose.yml`.
5. Пошаговый план фазы 1.

---
---

# ПРИЛОЖЕНИЕ Б. БЮДЖЕТ СЛОЖНОСТИ И БЕЗОПАСНОСТЬ

> Это приложение имеет приоритет над разделами 2 и 5 при конфликте. Если требование раздела 5 ведёт к превышению бюджета сложности — побеждает приложение Б.

## Б.1 Бюджет сложности

**Жёсткие лимиты фазы 1:**
- не более **5 000 строк** прикладного кода без учёта тестов, конфигов и демо-данных;
- не более **8 пакетов** в `packages/`;
- не более **3 реально работающих внешних адаптеров**, остальные — только моки;
- любой файл длиннее 400 строк подлежит разбиению;
- любая функция длиннее 50 строк подлежит разбиению.

Если бюджет превышен — сокращай функциональность, а не качество. Перед добавлением любой сущности отвечай на вопрос: без неё демо-сценарий сломается? Если нет — в roadmap.

### Правило абстракции
**Не создавай интерфейс, пока нет двух реализаций.** Исключения, где две реализации есть по факту и абстракция обязательна:

| Интерфейс | Обоснование |
|---|---|
| `LLMProvider` | GigaChat, YandexGPT, Claude, Mock — четыре реализации, ключевое требование импортозамещения |
| `IdentityProvider` | СУДИР + Mock |
| `UrbanDataProvider` | ИАИС ОГД + Mock |
| `DiffEngine` | растровый + DXF |

Всё остальное пиши прямыми функциями. `TransitionAnalyzer` — не иерархия классов, а функция, принимающая тип перехода и возвращающая набор правил из словаря. `scoring` — одна функция с конфигом весов, не пакет.

### Сокращённый состав пакетов (заменяет таблицу раздела 5)
```
packages/
├── llm_core/      LLMProvider, роутер, адаптеры
├── documents/     приём, парсинг, OCR, source_ref   (ingest + parsing)
├── analysis/      переходы, diff, правила, LLM-слой, скоринг, карта внимания
├── norms/         нормативная база и поиск
├── integrations/  identity (СУДИР), urban_data (ИАИС ОГД), моки прочих
├── audit/         журнал, provenance
└── api/           FastAPI-роуты, RBAC, схемы
```
Семь пакетов вместо тринадцати. `reporting` и `attention` — модули внутри `analysis`.

### Что переносится в roadmap без реализации
IFC-diff, реальные коннекторы 1С / ЭДО / СОД / ФГИС ЦС / СРО, граф связей, мультитенантность, дообучение моделей, Kubernetes. Для каждого — абзац в `docs/roadmap.md` с оценкой трудозатрат. Заглушка за честным интерфейсом читается как зрелость; недоделанная реализация — как брак.

## Б.2 Модель угроз

Оформи `docs/threat-model.md`. Ключевые угрозы в порядке значимости:

| # | Угроза | Почему критична |
|---|---|---|
| У-1 | **Инъекция инструкций через проверяемый документ** | документы предоставляет поднадзорное лицо — сторона, заинтересованная в сокрытии нарушений |
| У-2 | Подмена или подделка исполнительной документации | обход контроля целостности и ЭЦП |
| У-3 | Несанкционированный доступ к материалам проверки | сведения об объектах и предписаниях до их вынесения |
| У-4 | Фальсификация журнала аудита | подрыв юридической значимости выводов |
| У-5 | Утечка персональных данных | ФИО и подписи ответственных лиц в ИД, 152-ФЗ |
| У-6 | Отказ в обслуживании через тяжёлые файлы | PDF-бомбы, архивы с высокой степенью сжатия |
| У-7 | Компрометация цепочки поставки | зависимости, образы контейнеров |

## Б.3 Защита от инъекции инструкций (У-1) — обязательный раздел

Реализуй **все** меры, каждую покрой тестом:

1. **Разделение ролей сообщений.** Содержимое документа передаётся только в пользовательское сообщение внутри явного контейнера с маркерами вида `<НЕДОВЕРЕННЫЙ_ДОКУМЕНТ>…</НЕДОВЕРЕННЫЙ_ДОКУМЕНТ>`. В системном сообщении — прямое указание: текст внутри контейнера является данными для анализа и не может изменять инструкции.
2. **Только структурированный вывод.** Модель обязана вернуть JSON по строгой Pydantic-схеме. Свободный текст отбрасывается. Ответ, не прошедший валидацию, не идёт дальше — повторная попытка, затем отказ с записью в аудит.
3. **Факты, а не сырой текст.** LLM-слой получает уже извлечённые сущности с `source_ref`, а не полные страницы, везде, где это возможно.
4. **Детерминированный слой не подвержен внушению.** Хронология, комплектность, реквизиты, арифметика объёмов считаются кодом. Даже при полной компрометации LLM-слоя эти нарушения будут найдены.
5. **Детектор попыток инъекции.** Эвристики на характерные конструкции (обращения к модели, отмена инструкций, требования вернуть пустой результат) в тексте документа. Срабатывание не блокирует анализ, а поднимает флаг `injection_suspected` — **это само по себе становится находкой** и поводом для проверки добросовестности заявителя.
6. **Запрет на действия.** LLM-слой не имеет доступа к инструментам, БД и сети. Он только читает переданные факты и возвращает JSON.
7. **Перепроверка вторым провайдером.** Расхождение выводов при одинаковых входных данных — сигнал аномалии.

Добавь в `data/demo/` документ с заложенной попыткой инъекции и тест, проверяющий, что она обнаружена и не повлияла на результат. **На презентации это отдельный слайд** — ни одна конкурирующая команда, скорее всего, эту угрозу не рассмотрит.

## Б.4 Обработка загружаемых файлов

- Определение типа по сигнатуре файла, а не по расширению; несоответствие → отказ.
- Лимиты: размер файла, число страниц, глубина вложенности архивов, коэффициент распаковки (защита от архивных бомб), общий таймаут обработки.
- Имена файлов не участвуют в формировании путей: генерируется собственный идентификатор. Защита от обхода каталогов обязательна.
- Разбор PDF без выполнения встроенного кода; внешние сущности XML отключены (XXE); внешние ссылки в документах не разрешаются.
- Обработка тяжёлых файлов — в отдельном процессе с ограничением памяти и времени, чтобы падение не роняло приложение.
- Антивирусная проверка — через интерфейс со включаемой реализацией; в демо мок, в проде реальный сканер.
- Оригиналы хранятся неизменяемыми, доступ только по подписанным ссылкам с коротким сроком жизни.

## Б.5 Доступ, данные, аудит

- Аутентификация только через СУДИР. Локальных паролей нет. Собственная криптография не пишется ни при каких условиях.
- **Авторизация проверяется на уровне доступа к данным, а не в интерфейсе.** Запрос без явной проверки прав не должен возвращать данные. Принцип «запрещено по умолчанию».
- Все запросы к БД — параметризованные, через ORM. Конкатенация SQL запрещена, покрой это правилом линтера.
- Секреты только в переменных окружения или хранилище секретов. В репозитории — `.env.example` с пустыми значениями. `gitleaks` в pre-commit и в CI.
- Логи не содержат содержимого документов, персональных данных и токенов. Персональные данные маскируются на уровне форматтера логов.
- Минимизация персональных данных: хранить только необходимое для проверки, срок хранения задаётся конфигом.
- Журнал аудита — append-only, каждая запись содержит хэш предыдущей (цепочка целостности). Попытка изменения обнаруживается проверкой цепочки. Отдельная роль «Аудитор» с доступом только на чтение.
- Каждый вывод модели хранится с `provenance`: провайдер, модель, версия промпта, идентификатор запуска, время.
- Транспорт только по TLS; заголовки безопасности и политика источников настроены; список разрешённых источников для CORS явный, без подстановочных символов.
- Ограничение частоты запросов на загрузку и запуск анализа.

## Б.6 Правовая безопасность выводов

- Система формирует **гипотезы**, а не заключения. Формулировки интерфейса и API это отражают.
- Ни одно юридически значимое действие не совершается автоматически. Акт и предписание — только через подтверждение инспектором.
- Уверенность модели отображается всегда и никогда не округляется до «да/нет».
- Вывод без ссылки на пункт норматива и координаты в документе не сохраняется и не показывается.

## Б.7 Автоматическая проверка в CI

Настрой `.github/workflows/security.yml`, запуск на каждый коммит:

| Инструмент | Проверка |
|---|---|
| `ruff` | стиль, часть небезопасных конструкций |
| `bandit` | статический анализ Python на уязвимости |
| `semgrep` | правила OWASP + собственные правила проекта |
| `pip-audit` | известные уязвимости зависимостей |
| `gitleaks` | секреты в истории и в коммитах |
| `trivy` | уязвимости образов контейнеров |
| `import-linter` | нарушения границ модулей |

Зависимости зафиксированы по версиям и хэшам. Сгенерируй SBOM в формате CycloneDX. Вынеси статус проверок в README — на защите это работает.

## Б.8 Дополнение к критериям готовности

- [ ] Прикладной код укладывается в 5 000 строк, пакетов не более 8
- [ ] Нет ни одного интерфейса с единственной реализацией, кроме обоснованных в Б.1
- [ ] Тест подтверждает: попытка инъекции в документе обнаружена и не повлияла на результат
- [ ] Тест подтверждает: запрос к чужому объекту возвращает отказ на уровне данных
- [ ] Тест подтверждает: цепочка хэшей журнала аудита ломается при изменении записи
- [ ] Все семь проверок CI зелёные, SBOM сформирован
- [ ] `docs/threat-model.md` заполнена по угрозам У-1…У-7
- [ ] В логах при полном прогоне демо отсутствуют персональные данные и содержимое документов

---
---

# ПРИЛОЖЕНИЕ В. ПРОДУКТОВАЯ ПОЛНОТА БЕЗ УТЯЖЕЛЕНИЯ КОДА

> Отменяет и заменяет раздел Б.1 «Бюджет сложности». Требования безопасности Б.2–Б.8 сохраняют полную силу и сокращению не подлежат.

## В.1 Принцип распределения бюджета

Сложность бывает двух видов, и путать их нельзя:

- **Структурная сложность** (слои, интерфейсы, сервисы, адаптеры) — растёт нелинейно, порождает связанность, съедает время на отладку. Её режем беспощадно.
- **Поверхностная сложность** (экраны, компоненты, состояния интерфейса) — растёт линейно, изолирована, не создаёт связанности. На неё бюджет и тратим.

**Правило:** тонкое плоское ядро, богатый интерфейс, щедрые правдоподобные данные.

### Пересмотренные лимиты
| Область | Лимит | Комментарий |
|---|---|---|
| Прикладной код бэкенда | ≤ 5 000 строк | плоские функции, минимум слоёв |
| Пакетов в `packages/` | ≤ 8 | состав по Б.1 |
| Интерфейсов Port/Adapter | 4 | `LLMProvider`, `IdentityProvider`, `UrbanDataProvider`, `DiffEngine` |
| Фронтенд | **без жёсткого лимита** | дисциплина компонентов вместо лимита строк |
| Моки и демо-данные | **вне бюджета** | чем богаче, тем лучше |

## В.2 Витрина и ядро: три уровня глубины

Каждый экран из раздела 4 **существует и выглядит завершённым**. Различается только то, что под ним.

**Уровень A — живой сквозной сценарий** (реальная обработка, ради этого всё):
1. Вход через СУДИР (мок) → выбор роли
2. Новый анализ: ввод номера разрешения → подтягивание карточки объекта → загрузка ПД и РД → прогресс по этапам
3. Журнал расхождений с двухпанельным сравнением «было / стало»
4. Журнал гипотез → карточка гипотезы → переход к области на листе документа
5. Карта внимания → печать → мобильный офлайн-режим
6. Оценка инспектора → формирование проекта акта → выгрузка файла

**Уровень B — реальные данные, упрощённая логика** (работает на подготовленном наборе): дашборд с метриками, журнал анализа документации, журнал аудита с проверкой цепочки хэшей, история проверок объекта, настройки правил.

**Уровень C — витрина** (полноценный экран на моках, честно помечен в интерфейсе и в документации): настройки внешних интеграций со статусами подключения, справочник классификатора нарушений, управление пользователями и ролями, аналитика по подрядчикам.

**Обязательно:** уровень C помечается ненавязчиво — статусом «демонстрационные данные» в углу экрана. Честность здесь не снижает впечатление, а повышает доверие; обнаруженный жюри необъявленный муляж — снижает необратимо.

## В.3 Продуктовые элементы коммерческого уровня

Дёшево в реализации, решающе для впечатления. Реализовать все:

**Первый экран после входа — дашборд, а не пустой список.**
- Крупная цифра: сэкономлено человеко-часов за период (расчёт из числа обработанных документов × нормативное время ручной проверки, формула в конфиге и раскрывается по клику)
- Обработано документов, найдено гипотез, подтверждено инспекторами, доля подтверждения
- Объекты ближайших выездов с числом критических расхождений
- График динамики за 6 месяцев

**Формирование документов.** Проект акта проверки и предписания с реальной выгрузкой в файл, с реквизитами, ссылками на нормативы и приложением доказательств. Не «скачать заглушку» — настоящий документ.

**Полировка интерфейса, отличающая продукт от прототипа:**
- скелетоны при загрузке вместо спиннеров, оптимистичные обновления
- осмысленные пустые состояния: что произошло и что сделать дальше
- тосты подтверждения на каждое действие, отмена последнего действия
- глобальный поиск по объектам, документам и гипотезам с горячей клавишей
- сохранение фильтров и настроек колонок между сессиями
- хлебные крошки, корректная работа кнопки «назад», ссылки на конкретные записи
- переключение плотности таблицы, экспорт видимой выборки
- тёмная тема — опционально, но заметно повышает восприятие зрелости

**Экран администратора.** Переключение LLM-провайдера, включение и отключение правил детектора, веса скоринга, пороги уверенности, состояние интеграций. Этот экран сильнее прочих создаёт ощущение промышленной системы — и он же место для живой демонстрации переключения на GigaChat.

**Журнал аудита как видимый экран** с кнопкой «Проверить целостность цепочки» и наглядным результатом.

## В.4 Данные решают больше, чем код

Демо-данные — часть продукта, не тестовый материал.

- 3 объекта капитального строительства с реальной географией Москвы, правдоподобными застройщиками, этапами и сроками
- 1 500–2 000 документов в счётчиках, из них 40–60 действительно обрабатываются
- история проверок с датами, предписаниями и сроками устранения за 8 месяцев
- ФИО инспекторов и ответственных лиц — вымышленные, но естественные
- метрики дашборда согласованы между собой: сумма по объектам сходится с общей цифрой

Никаких «Объект 1», «файл без имени», «Иванов И.И.». Несогласованные или шаблонные данные обнуляют впечатление от любой архитектуры.

## В.5 Коммерческая перспектива — отдельный раздел презентации

Задача предполагает масштабирование по стране. Заложи это в продукт и покажи:

- **Мультирегиональность через конфигурацию:** нормативная база, классификатор нарушений и формы документов вынесены в конфиг региона. Подключение нового субъекта — загрузка нормативного пакета, а не доработка кода. Продемонстрируй переключением региона в настройках.
- **Модель поставки:** размещение в защищённом облаке региона либо в контуре заказчика; лицензирование по числу инспекторов или объектов.
- **Экономика:** расчёт эффекта на основе сокращения времени подготовки к выезду. Формула прозрачна и приведена в `docs/economics.md`, а не заявлена голословно.
- **Расширение периметра:** та же машина применима к экспертизе проектной документации, к строительному контролю заказчика и к приёмке. Один слайд, без реализации.

## В.6 Перераспределённый план 14 дней

| День | Задача |
|---|---|
| 1–2 | Демо-данные: комплекты ПД/РД, ИД, заложенные расхождения, `ground_truth.json`, таксономия нарушений из публичных источников |
| 3 | Каркас: репозиторий, БД, 4 интерфейса, docker-compose, CI с проверками безопасности |
| 4–5 | `documents`: приём, парсинг, OCR, `source_ref`, защита от инъекций (Б.3) |
| 6–7 | `analysis`: переходы, растровый diff, правила, LLM-слой, скоринг |
| 8 | `norms`: нормативная база, поиск по пунктам, контроль актуальности редакций |
| 9 | API, RBAC, мок СУДИР и ИАИС ОГД, аудит с цепочкой хэшей |
| 10–12 | **Фронтенд: все экраны трёх уровней, полировка, дашборд, выгрузка документов, мобильный офлайн** |
| 13 | `evaluate.py`, метрики, `docs/` (архитектура, модель угроз, экономика, roadmap) |
| 14–15 | Только презентация: сценарий, запись видео, слайды, репетиция. Код не трогать |

Три дня на интерфейс — не избыточность, а перенос бюджета с выброшенных слоёв бэкенда.

## В.7 Что режется первым при нехватке времени

Строго в этом порядке:
1. Тёмная тема
2. Аналитика по подрядчикам (уровень C)
3. Управление пользователями (уровень C)
4. Двухпанельное сравнение → одна панель с подсветкой
5. Мобильный офлайн → адаптивная вёрстка без офлайна

**Не режется ни при каких условиях:** защита от инъекций и тест на неё, `evaluate.py` с метриками, `provenance` у каждого вывода, ссылки на пункты нормативов, дни 14–15 на презентацию.

---
---

# ПРИЛОЖЕНИЕ Г. МЕХАНИКА СРАВНЕНИЯ КОМПЛЕКТОВ

> Сжатая справочная версия. Каждое правило выведено из разбора реальных
> комплектов (в основном ОДИН комплект одного раздела — статус **n=1**
> почти везде, см. Г.21) и применимо к любому комплекту ПД/РД/ИД.
> **Полный разбор с доказательствами, цитатами и историей прогонов —
> `docs/PRILOZHENIE-G-ISTORIYA.md`, тот же номер Г.N.** Здесь — только
> итоговое правило и где оно реализовано; при любом сомнении в нюансе
> смотреть полную версию, не додумывать по заголовку.

## Г.1 Два бэкенда — где что чинить *(ОТМЕНЁН Г.85: старый бэкенд удалён, движок один — правило оставлено по append-only, применять НЕ нужно)*
`packages/api`(+`documents`/`analysis`/`llm_core`/`norms`/`integrations`/`audit`) — полная CRM по разделам 2–5. `packages/backend` (порт 8010) — лёгкий инструмент реального сравнения документов, **сюда идёт вся практическая реализация** этого приложения. Кодовые базы не связаны — перед новым кодом проверить, нет ли уже реализации в другом бэкенде.

## Г.2 Что из полной CRM не портировано
Перепроверка вторым провайдером (`analysis/verification.py`) — применять точечно, не на всём объёме. `DiffEngine` (растровый) — бесполезен для T3 (ПД↔РД): разные жанры чертежа, разный масштаб листа.

## Г.3 Локальный провайдер — родной `/api/chat`
Ollama через OpenAI-совместимый `/v1/chat/completions` молча игнорирует `num_ctx` → запрос падает на контексте. Для `provider="local"` — `POST /api/chat`, картинки base64 в `message["images"]`, размер контекста через `options.num_ctx`.

## Г.4 Порядок сопоставления: сверху вниз, от смысла к зоне
Штамп → что это за лист (ГОСТ Р 21.1101) → сопоставление по смыслу наименования → якорь внутри листа (Г.5) → зона якоря (Г.7). Каждый шаг дороже предыдущего и уже по числу листов. Не подпирать непрочитанный штамп эвристиками по ключевым словам — читать штамп (`stamp.py`/`stamp_vision.py`).

## Г.5 Якорь сшивки зависит от жанра листа
Общий якорь физически разный: план↔план (АР/ОВ/ВК/ЭОМ) — номер помещения; план↔план (КР/КЖ/КМ) — оси+отметка; схема↔план — обозначение системы; текстовые листы — номер документа/марка. Не один зашитый якорь, а набор извлекателей. **Нет общего якоря с обеих сторон — «листы несопоставимы», а не сравнение вслепую.**

## Г.6 Одному листу соответствует НЕСКОЛЬКО листов
Схема охватывает все этажи, планы — по одному на этаж. Лист ПД сопоставляется со ВСЕМИ листами РД, где встречаются его помещения/системы, и сравнение идёт по кропу на КАЖДЫЙ якорь, не одному кропу на пару. Беглый просмотр целого листа — не замена кропу на каждый якорь; если бюджета не хватает — явно фиксировать «не выполнено» (Г.10), не заменять молча.

## Г.7 Сравнивается зона якоря, а не лист целиком
Расхождение, невидимое на полном листе, читается на вырезанном кропе сразу — подписи чертежа обычно векторные, кроп рендерится чётко при любом увеличении. Открытый вопрос — чем ограничивать зону (привязка к реальной границе, не наивный радиус, см. Г.22). `vision.py`/`compare_page_pair` — вызов внешнего провайдера, нужен ГОТОВОМУ ПРОДУКТУ; сессия Claude Code, ведущая анализ, сама мультимодальна и может рендерить/смотреть зону напрямую (`page.get_pixmap` + чтение файла), без внешнего провайдера.

## Г.8 Текстовый слой может быть частичным — опаснее пустого
CAD-экспорт часто переводит подписи в кривые; `get_text` при этом возвращает не ноль, а мусор (рамка, экспликация) — лист выглядит прочитанным, а сравнивать почти нечего. Бедный текстовый слой — обязано быть видимым состоянием пары, не молчаливым «расхождений нет».

## Г.9 Помещения: три категории, а не пересечение
Есть с обеих сторон → сравнить зону. Только в ПД → искать пару среди «только в РД» по названию+площади (перенумерация) или заявить «отсутствует». Только в РД → зеркально. Площадь — сильный признак перенумерации. **Названия не сравнивать строкой** (сокращение vs полное имя — ложные «переименования»); название годится как признак сопоставления, не как расхождение само по себе.

## Г.10 Тишина никогда не означает «чисто»
Сорванный вызов модели, пустой текстовый слой, исключённый лист, позиционная пара низкой уверенности — каждый пропуск ОБЯЗАН быть видимым состоянием с причиной, не молчаливым «расхождений нет».

## Г.11 Любой фильтр отсева — только по наблюдённому ложному срабатыванию
Каждое правило `rooms.py`/`material.py` и любого будущего фильтра — сначала наблюдение на живом комплекте, потом правило, потом тест на это наблюдение. Не «на всякий случай»: фильтр без основания режет настоящие данные, что дороже шума.

## Г.12 Как измерять прогресс — слепой прогон
Порядок обязателен: механика отрабатывает сама (без наведения на известные места) → результат фиксируется письменно → только потом открывается эталон. Если знание эталона неустранимо — мера прогресса это объективные величины (доля верных пар, число кандидатов), не сам факт находки. **Письменный отчёт слепого прогона не коммитить в репозиторий** — утечка эталона следующей слепой сессии тем же путём, что и открытие файла ответов напрямую.

## Г.13 Зона штампа: искать по содержимому, не угадывать геометрию
Разные форматы листа и формы штампа (ГОСТ Р 21.1101, форма 2/3) не дают одного прямоугольника. Порядок: искать метки штампа («Стадия», «Листов», «Формат») по тексту → если текст богат, но меток нет — штампа на странице действительно нет → если текста почти нет, различать CAD-экспорт (много векторных путей, узкая точная зона под зрение) от фотоскана (один растр на весь лист, широкая зона треть-четверть листа). Каждый путь — видимое состояние результата.

## Г.14 Голые номера на плане — вето-признак, не реестр
Голые номера-подписи у контура (без названия) на порядки многочисленнее настоящих помещений — как реестр это чистый шум. Используются ТОЛЬКО как вето перед заявлением «есть только в ПД/РД» (Г.9): если пропавший номер встречается голой подписью на плане другой стороны — это бедный текстовый слой, не расхождение.

## Г.15 Экстрактор «система ↔ помещение» обязателен
Мало сравнить множества обозначений систем (Г.5) — «система X есть в ПД, нет в РД» неадресуемо. Список обозначений на схеме почти всегда стоит перед номером/названием помещения — разбирать как отдельный факт «система → помещение», превращая находку в «в помещении N отсутствует система X». Система, не встречающаяся вообще ни разу на одной стороне, — критичность высокая по умолчанию, понижать только по разобранному случаю.

## Г.16 Разделение/объединение системы — по числу физических единиц
Подпись «X.1/X.2, открыта/закрыта дверь» может быть режимами ОДНОЙ установки (расчётный вариант), не двумя установками. Отличать по ведомости объёмов РД: один индекс = не находка; отдельные строки с разным расходом/напором = настоящее разделение.

## Г.17 Комплектность — по документу «Состав документации», не по кодам норм
Документ «Состав [ПД/РД] документации» даёт машиночитаемый список обязательных комплектов — сверять текстовые ссылки других документов на раздел/обозначение с этим списком и с фактически переданными файлами (та же трёхкатегорийная логика Г.9, уровнем выше). **Не** сверять литеральные коды СП/ГОСТ построчно между сторонами — расчётный раздел и справка о соответствии разного назначения, не пересказ одного текста другим (Г.11: нет наблюдённого ложного срабатывания на этом — не строить проверку).

## Г.18 Адресация листов — пробег вперёд до штампа, не константа
«Лист N» из акта ≠ номер страницы PDF; нумерация листов сбрасывается на каждой смысловой границе документа. Алгоритм: идти вперёд от начала раздела, читать номер листа из штампа (текст → зрение); как только получены 2 согласованных попадания подряд — смещение установлено для этого раздела; дальше применяется без повторного чтения каждого листа. Смещение может отличаться по величине и по сторонам комплекта (административная преамбула перед нумерацией листов).

## Г.19 Якорь в прозе — искать не только в структурированном реестре
Тот же номер/обозначение часто повторяется в свободном тексте (ПЗ, «Ведомость изменений») — для каждого якоря, уже подтверждённого реестром/сопоставлением, отдельным дешёвым шагом искать голым токеном по прозе. **Не голым свипом по всему корпусу** — тонет в шуме каталога поставщика (сотни ложных совпадений на короткие номера); рабочий вариант — сначала фильтр по словам смены («заменен», «исключен», «ведомость изменений»), потом якорь на отфильтрованном, и только по страницам `text_facts` (не сырой текст файла). Реализовано: `anchor_prose.py` (`find_anchor_in_prose`).

## Г.20 Ведомость оборудования — тот же реестровый паттерн, что помещения
Спецификация оборудования разбирается как экспликация (Г.9): код позиции (с родительско-дочерней нумерацией «14»→«14.1»), три категории. Реализовано: `equipment.py` (`extract_equipment_facts`), принимает `room_keys` уже найденные `rooms.py` на той же странице и не регистрирует совпадающий ключ повторно — короткие числовые коды позиции и номера помещений неотличимы регуляркой без этого фильтра (подтверждено первой слепой проверкой).

## Г.21 Статус правил Г.13–Г.20 — все n=1
Выведены из разбора ОДНОГО комплекта. Таблица риска непереноса на другой документ: низкий (Г.13 метки ГОСТ, Г.17 документ по ГОСТу, Г.18 сброс нумерации по ГОСТу, Г.19 механика поиска без словаря) → средний (Г.15 конвенция подписи схемы, Г.22 факт разделения текст/графика логичен, но эвристика распознавания не проверена) → высокий (Г.20 формат таблицы спецификации плывёт от раздела к разделу). Обновлять таблицу по факту проверки на втором комплекте, не переписывать задним числом.

## Г.22 На большом плане якорь и содержательная зона — разные места листа
На листе большого формата текстовый якорь (служебная таблица в углу листа) и содержательная графика (сотни линий инженерных сетей или конструктивных элементов) физически разделены — прочитанная таблица не заменяет просмотр графики. Правило: у `PAGE_KIND_DRAWING` минимум два подтипа — компактная схема/легенда (якорь рядом с содержанием, кроп на якорь достаточен) vs полноформатный план со служебной таблицей (после текстового совпадения по таблице обязателен ЛИБО рендер графической части целиком, ЛИБО прицельный поиск контура помещения по векторной подписи у самого контура). Различитель подтипа в коде пока не формализован.

## Г.23 Одна находка в зоне — не повод считать зону разобранной
Найти один настоящий факт в верно определённой зоне и остановиться — систематическая ошибка на трёх разных масштабах (Г.6 — не докропили якорь; Г.22 — докропили таблицу, не заглянули в графику; Г.23 — докропили верно, не проверили второй факт рядом). Правило: зона разобрана не при первой находке, а когда проверено, что это самый релевантный факт зоны — обязателен второй целенаправленный проход.

Практические приёмы (принцип словами недостаточен — проверено повторно):
1. Обязательное поле «второй проход» в отчёте на каждую зону с находкой (пусто = незакрыто).
2. Сначала вширь по ВСЕМ уверенным парам (полный список кандидатов), потом вглубь по каждой — иначе бюджет кончается на первой паре.
3. Явный вопрос сразу после находки: «что ещё в этой зоне могло бы быть тем нарушением?»
4. Фаза «вглубь» — строго по убыванию скора пары, не по удобству; обрезается низкоприоритетный хвост, не середина.
5. Разбор большой многопомещенческой пары — перечислимый чек-лист из реестра (Г.9), не свободный визуальный обзор: каждый номер — отдельная строка «проверено: да/нет».

Реализовано: `deep_dive_checklist.py` (структура вместо напоминания) → недостаточно (список виден целиком, искушение сойти с порядка) → `deep_dive_session.py` (`DeepDiveSession`, пошаговый интерфейс, `current()` отдаёт ТОЛЬКО текущий пункт). Приоритет внутри перечня: якорь Г.19 → индивидуально подписанная ветка системы (ОВ-специфичное наблюдение, см. Г.42 обобщение) — но это приоритет ВНУТРИ пары, не отменяет очерёдность ПАР по скору (см. Г.27).

## Г.24 Примеры в методике — обезличенные, без референса на объект
Любая идентифицирующая деталь в тексте методики (конкретный лист/помещение/формулировка, и даже точное числовое значение скора — детерминированный матчинг делает число координатой) долетает до следующей «слепой» сессии раньше эталона, потому что промт требует прочитать CLAUDE.md целиком. Правило: описывать тип ошибки/паттерн, не адрес находки; агрегированные метрики (доля, общее число) — годятся без обезличивания, точные значения, указывающие на одну пару/находку — нет.

## Г.25 Чек-лист гарантирует порядок, не глубину
`DeepDiveSession` прошёл все пункты без пропуска — но «проверено» означало и дешёвую текстовую сверку, и настоящий визуальный кроп, в одном булевом поле. Правило: `resolve(depth, finding)` требует явно назвать способ (`DEPTH_TEXT`/`DEPTH_VISUAL`) без дефолта; `status()` считает кропы отдельно от закрытых пунктов. Полный текстовый проход — уже полезная работа сама по себе, но не заменяет визуальный, которого не бывает поровну на всех пунктах — визуальный кроп резервируется для приоритетных (якорь/индивидуальная подпись).

## Г.26 Матчинг листов 1-к-1, а Г.6 требует 1-ко-многим
`match_page_pairs` — жадное сопоставление, не более одной пары на лист с каждой стороны — реальный пропуск: якорь Г.19 указал на лист, который жадный алгоритм уже отдал другой паре. Реализовано: `anchor_pages.py` (`find_anchor_pages`/`render_uncovered_report`) — по номеру помещения находит ВСЕ страницы РД независимо от исхода жадного матчинга, с пометкой покрытия уверенной парой.

## Г.27 Структурный гейт (Г.25) не гарантирует, что им пользуются
`DeepDiveSession` не позволяет перепрыгнуть пункты ВНУТРИ себя — но ничто не мешает вообще не запустить сессию для приоритетной пары, открыв нужный лист вручную по наводке якоря раньше. Приоритет пары по скору (приём №4, Г.23) остаётся требованием к ПОВЕДЕНИЮ аналитика, инструмент это не гарантирует — только честно фиксировать отклонение в отчёте.

## Г.28 Топ-слот ушёл на непроектный материал; Г.23 — снова
Внутренняя страница подшитого каталога поставщика (построчные цены + «Итого по», без прежних признаков колонтитула) проходила фильтр `material.py` и занимала лучший скор `DeepDiveSession`. Исправлено: третий признак — ценовые токены + маркер подытога. Отдельно: автоматический текстовый отчёт, уже пометивший номер как «есть с одной стороны» В ТОЙ ЖЕ зоне с подтверждённой находкой, — не шум по умолчанию, требует явного решения (проверен/отклонён), не группового списания.

## Г.29 Реестр присутствия структурно слеп к нарушениям маршрутизации
Три класса проверки: (1) требование в 3 независимых местах документа — дёшево и надёжно, только текст; (2) маршрут «помещение→ветка→точка сбора» — разные номера веток при том же перечне помещений не нарушение сами по себе, находка — в том, что маршрут ведёт к ДРУГОЙ точке сбора/другому числу присоединений — видно только прямым трейсингом, ни текстовый поиск, ни diff множеств ключей этого не видят; (3) раздвоенный код позиции оборудования — не разрешается без строки спецификации. Контрольный эксперимент: `registry_diff.py` по всему комплекту не выдал НИ ОДНОГО подтверждённого нарушения класса 2 — все помещения присутствуют с обеих сторон, реестр по конструкции слеп к рёбрам графа.

## Г.30 Чего не хватает для детектируемости класса 2 (маршрутизация)
По цене/отдаче: (0) ужесточить фильтр `equipment.py`; (1) экстрактор баланс-рамки приток/вытяжка (`balance_box.py`/`balance_vision.py`, **ОВ-специфично**, не общий механизм); (2) координатный реестр между стадиями (`coord_registry.py`); (3) граф маршрутизации `room→ветка→точка сбора` по геометрии (`routing_graph.py`, узлы с несколькими кандидатами → `ambiguous` → зрение; распознавание кодов веток тоже **ОВ-специфично**); (4) триангуляция — `confirmed` только при ≥2 независимых источниках (`triangulation.py`); (5) классификатор безобидного/реального расхождения + пакет эскалации человеку, не вердикт модели (`escalation.py`). Зрение — последний шаг эскалации, не первый источник.

## Г.31 Обобщение якоря за пределы номера помещения
На листах без экспликации (сечения, узлы, чисто конструктивные планы) вся механика Г.30 молчала бы. Реализовано: `axes.py` (оси+отметки, кластеризация разрывом по координате, порог кластера ≥5 после ложного совпадения на меньшем пороге), `dimensions.py`/`dimension_vision.py` (размеры у ЛЮБОГО якоря), `routing_graph._nearest_anchor` (несколько типов якоря одновременно, ближайший физически побеждает; поле осталось `room_key`, задокументировано как «ключ ближайшего якоря»).

## Г.32 Реестр требований из прозы ПД — второй тип извлечения
Часть требований живёт только в свободной прозе ПЗ, без строки в таблице. Две формы: (1) тире-пункт списка с коротким кодом системы в скобках — регулярная, десятки пунктов; (2) требование БЕЗ кода, номера в скобках + глагол-предикат («предусмотрен(о/а)»/«предусматривается», намеренно узкая регулярка — расширение списка глаголов без нового измерения не стоит риска ложных находок). Реализовано: `requirement_registry.py`.

## Г.33 Сверка реестра требований с РД — два метода
Требование С кодом: код ищется как отдельное слово в полном тексте РД (присутствие — не сверка списка помещений). Требование БЕЗ кода: сверять нечем текстом — ВСЕГДА кандидат на зрение (`no_code_visual_check_needed`), текстовое отсутствие в РД не равно отсутствию на чертеже. Реализовано: `requirement_cross_check.py`; сигнал в триангуляцию только по находкам «не найдено», не по «подтверждено».

## Г.34 Узкий поиск кода-токена не наследует фильтр material.py
Один и тот же файл, две разные по риску операции над текстом: парсинг строки реестра (шум каталога дорог, `material.py`-фильтр верен) vs присутствие короткого токена (шум каталога почти невозможен, фильтр может вырезать единственный текстовый след кода, подшитый внутри каталога). Решение — не менять `material.py`, а у операции поиска токена использовать нефильтрованный источник текста (`registry_diff._load_text_facts`, не `extract_document_facts`).

## Г.35 Эскалация требования-кандидата в зрение — весь лист целиком
Несоответствие требования без кода может быть разлито по всему изображению помещения — заранее вычисленный координатный кроп рискует не захватить нужную часть. Решение — отдать модели весь лист (`render_page_to_data_url`, без новой геометрии). Три вердикта: confirmed/absent/unclear — не гадать при сомнении, ложный «absent» дороже честного «не могу судить». Кандидатные страницы — round-robin по помещениям требования, вширь, не вглубь по одному. Реализовано: `vision_page_compare.py`.

## Г.36 Regex-экстрактор требований написан под один документ
Прямая пользовательская поправка: «примеры были иллюстрацией, не спецификацией». Regex Г.32 ловит только то, что видела, — следующий документ (другой раздел/автор/формат списка) пройдёт мимо молча. Решение — не патч регулярки, а смена инструмента: `requirement_llm_extract.py` — тот же результат (`Requirement`), но ЛЛМ-вызовом по обязывающему смыслу, без опоры на конкретный маркер/глагол. `requirement_cross_check.py` принимает готовый `list[Requirement]` — сверка не знает источника. `requirement_registry.py` остаётся узким запасным путём без ключа ЛЛМ (`RUN-NO-LLM.bat`).

## Г.37 known_violations.json — few-shot, не дообучение весов
«Подготовить модель к обучению» = датасет размеченных примеров в системный промпт, не fine-tuning весов (недоступно, нет договора/датасета). Механизм уже существовал (`vision.py`), но `requirement_llm_extract.py`/`vision_page_compare.py` (обе написаны в этой же сессии) забыли его подключить — исправлено (`known_violations_block()`). **Пополнять только нарушением с ДРУГОГО объекта**, не с того, что проверяется вслепую сейчас — иначе переобучение под видом общего правила и утечка эталона в промпт.

## Г.38 GigaChat: путь `/v1/`, картинка не инлайн
Базовый вызов — `POST /v1/chat/completions` (версия в пути). Картинка — НЕ инлайн (не `image_url` как у OpenAI/Anthropic/Google), а отдельная загрузка: `POST /v1/files` (multipart) → `id` → `attachments` сообщения. Рабочая копия `llm.py` успела разойтись с собственным обоснованным тестом репозитория (`test_llm.py`) — источником истины выбран коммит с тестом и явным комментарием, не «более новая» версия. Урок: контракт внешнего API, который нельзя проверить живым вызовом, защищает только тест, зафиксированный коммитом, и сверка новой правки с ним.

## Г.39 Первый живой вызов GigaChat подтвердил Г.38
С машины пользователя: OAuth, текст, vision (`POST /v1/files`+`attachments`), полный `scan_cli.py` на реальном комплекте. Контракт Г.38 подтверждён API, не только тестом. Два новых факта: GigaChat отклоняет `response_format:{"type":"json_object"}` (просить JSON текстом промпта, не полем); может понадобиться `GIGACHAT_CA_BUNDLE=false` на машине без НУЦ Минцифры в доверенном наборе ОС.

## Г.40 Экспликация и план — на РАЗНЫХ страницах, не только зонах
Более тяжёлый вариант Г.22: план и его экспликация физически далеко друг от друга, без прямой текстовой привязки. `room_facts` намеренно не содержит голых номеров с плана (Г.14) — эскалация в зрение получала только таблицу, не чертёж. Резерв: отметка высоты этажа (`+X.XXX`) почти всегда остаётся текстом даже там, где остальная графика в кривых — находит ВСЕ страницы `page_kind=drawing` с той же отметкой (не гарантированно то же помещение, только тот же этаж; решает по-прежнему зрение). Реализовано: `level_pages.py`, кандидаты добавляются ПОСЛЕ найденных по номеру.

## Г.41 Сводка и вердикты — на диск сразу, не одним куском в конце
Долгий прогон (минуты) терял промежуточный результат при прерывании. Правило: каждая часть пишется на диск сразу по готовности — `render_requirements_summary` печатается до сверки; `check_visual_candidates` принимает `on_result`, вызывается по одной находке. `registry_diff.py --out ФАЙЛ` — `flush()` после каждой записи. `RUN-VISION.bat` передаёт `--out` всегда.

## Г.42 Целевой аудит на overfitting — три реальных находки
По прямому запросу пользователя. Честный статус «n=1» — не overfitting (корректная документация эпистемического статуса); overfitting — когда узкая механика подана как общая БЕЗ пометки. Найдено и исправлено: (1) Г.23 приём №5 переформулирован как общий принцип, а не как пример из одного раздела; (2) Г.30 пп.1/3 явно помечены раздело-специфичными в тексте и докстрингах — пометка границы это честность, а не конкретика, и убирать её нельзя; (3) `matching.py` — эвристика различения подсистем стала гейтиться кодом раздела (единственная находка, где неточность была в ПОВЕДЕНИИ кода, не только в тексте; позже снята вовсе — Г.88).

## Г.43 Обзор комплекта — до погружения в содержимое
Даже после Г.42 оставалось впечатление «зашито под ОВ», потому что единственный вход — узкий `--kind` режим. Реализовано: `set_overview.py` — `summarize_volume`/`summarize_set` (без LLM, за секунды: раздел по ПП№87, объём, число помещений/позиций), `compare_section_coverage` (трёхкатегорийный принцип Г.9 на уровне разделов, не элементов). Подключено как `run_overview()`, печатается первым при любом запуске. Критерии допустимости отличий РД от ПД (ч.3.8 ст.49 ГрК РФ) — справочная рамка в `SEVERITY_RULE` промпта, не жёсткий код-гейт (юридическое суждение не задача автоматики, Б.6).

## Г.44 Прицельная сверка маршрутизации — вызываемый инструмент, упёрся в векторизацию
`routing_diff.py` доводит граф Г.30 п.3 до CLI (`--kind routing --rooms N,N,N`) — поиск кандидатных планов по номеру помещения (резерв — этаж, Г.40), построение графа, сравнение через `diff_routing_graphs`. Проверено на реальном комплекте: механика отработала верно на каждом шаге и честно вернула пустой граф — подписи веток («В2.7» и т.п.) на этом комплекте переведены в кривые, текстовый слой их не несёт. Не сбой, а вывод: нужен vision-читатель подписей веток (`KIND_BRANCH`/`KIND_TARGET`, аналог `balance_vision.py`), архитектура уже это предусматривает (`build_routing_graph(labels=...)`), сам читатель не написан.

*(Г.45 не заводился — нумерация продолжена с Г.46 в исходной истории сессии.)*

## Г.46 Модули были построены поодиночке — цепочка обрывалась на полпути
`room_cross_check.py`/`equip_cross_check.py`/`triangulation.py`/`escalation.py`/`router.py` — построены и протестированы, но ни разу не вызывались ни из `main.py`, ни из `registry_diff.py`: `--kind` был эксклюзивным выбором одного узкого пути. Реализовано: `run_triangulated()`, режим `--kind all` (новый дефолт) — кросс-проверка реестров (с severity, не просто diff множеств) + требования + routing (если заданы `--rooms`) → сигналы через `signals_from_*` → `triangulate()` → `escalation.build_tickets()` на кандидатов с одним источником. `run_no_llm.sh` переведён на `--kind all`.

## Г.47 Сводка требований видела единицы вместо сотен — форма 3
Форма 2 требует ОДНОВРЕМЕННО буквальное «(пом. N)» И корень «предусмотр-» — на реальном ПЗ (177 стр.) это дало 2 находки за весь документ, хотя реальных нормативных предложений («должен быть...», «следует...», «выполняется по ГОСТ...», требования без привязки к помещению) на два порядка больше. Реализовано: `extract_general_requirements`/`render_general_requirements_summary` — не требует «(пом. N)», ловит более широкий набор нормативных глагольных конструкций (должен/необходимо/следует/требуется/устанавливается/обеспечивается/выполняется). 84 находки против 2 на том же файле. **Каталог для человека, НЕ входит в `extract_requirements()`/автосверку/триангуляцию** — шире охватом и заведомо шумнее.

## Г.48 Сводка должна лежать в основе проверки РД, не оставаться каталогом
Прямое указание пользователя: форма 3 должна не просто читаться человеком, а реально проверяться против РД. Реализовано: `cross_check_general_requirements`/`_extract_token` — токен по приоритету: номер ГОСТ/СП/СНиП/СанПиН → название/марка в кавычках → фраза «класс ... «X»» ЦЕЛИКОМ (не голая буква — совпадёт с чем угодно). Позиции оборудования и числовые параметры сознательно не токены (короткий-код-шум как в Г.28 / вопрос сравнения значения, другая механика). На реальном комплекте: 14 из 84 с токеном (сверены текстом), 70 без токена — видимая пометка `no_token_manual_review`, БЕЗ автоматической эскалации в зрение (форма 3 шумнее формы 2).

## Г.49 Токен не находится в РД — предел поиска подстрокой, не брак
Пользователь подтвердил диагноз Г.48 и указал следующий шаг: сверка по СМЫСЛУ, не по совпадению символов. Реализовано: `requirement_text_verify.py` — ЛЛМ читает корпус текста РД пачками (та же разбивка, что `requirement_llm_extract.py`); вызов на ПАЧКУ (не на пару требование×пачка) со списком ВСЕХ ещё нерешённых требований разом — иначе 84 требования дают сотни вызовов. Требование выходит из списка, как только получило confirmed/absent на любой пачке; нерешённое ни на одной остаётся честным `unclear`. Подключено под `--verify-requirements`, работает только над требованиями, оставшимися после токен-сверки (Г.48). Живого GigaChat в среде разработки нет (Г.37) — проверено структурно (моки + сквозной прогон на реальном комплекте).

## Г.50 Комплексный прогон с ключом — текст и чертежи разом, не только текст
`--kind all` С ключом всё равно пропускал граф маршрутизации без явного `--rooms` — цепочка оставалась текстовой, хотя расхождение бывает и на чертеже. Реализовано: без `--rooms`, но с ключом, `run_triangulated` автоматически берёт до `MAX_AUTO_ROUTING_ROOMS` (15) помещений, УЖЕ отмеченных расхождением в реестре (Г.9) — точечно, не весь комплект (~6 с/лист, Г.30). Без ключа поведение прежнее (нужен явный `--rooms`) — не утяжеляет бесплатный `RUN-NO-LLM.bat`. `RUN-VISION.bat` переведён на `--kind all --verify-requirements`.

## Г.51 Прямая сверка чертежей ПД↔РД — независима от текстовых требований
Прямое указание пользователя: «проверять надо чертежи что в ПД что в РД». Механизм (`compare_page_pair`/`compare_text_pair`, Г.4-Г.8: показать паре листов ЛЮБОЕ расхождение без привязки к тексту-требованию) уже работал в FastAPI, но `--kind all` его не вызывал. Формы 1-3 идут ОТ текста К чертежу, этот путь — от пары листов напрямую, поэтому не может быть «шагом после текста». Реализовано: `run_page_pair_comparison()` — `match_page_pairs` + `router.classify_all_pairs` (level 0 отсекает совпавшие по реестру пары) → готовые `compare_*` из `vision.py`. Находки НЕ заведены в триангуляцию сознательно: свободный текст вывода модели не даёт надёжного `room_key` (в отличие от форм 1/2/3) — отдельная секция отчёта, не сигнал. Проверено на реальном комплекте: 121 пара выше уровня 0 без единой ошибки.

## Г.52 Эскалация в зрение (Г.35) рисковала не дойти до релевантного файла
Слепой прогон, сверенный с реальным перечнем нарушений: `_candidate_pages` отбирала листы «в ширину по помещениям» БЕЗ учёта того, что кандидатные листы одного помещения бывают из РАЗНЫХ файлов одной дисциплины, покрывающих разные подсистемы (наблюдённый случай: два тома одного раздела на разные подсистемы, расхождение видно только на одном из них). При бюджете `max_pages_per_finding=3` бюджет исчерпывался на листах одного не того файла. Исправлено: предпочитать страницу из файла, ещё не представленного среди отобранных; старое поведение — только когда таких нет.

## Г.53 equip_registry (Г.20) — три новых класса штампового шума
Точечная визуальная проверка кандидатов из того же слепого прогона подтвердила зашумлённость фактом, не гипотезой (Г.11). Три наблюдённых случая: отметка уровня с пределом огнестойкости рядом, наименование организации из штампа рядом с графами «Лист/Листов», заголовок «Состав рабочей документации» — во всех дата вида день.год читается как код позиции. 3 паттерна в `_REJECT_NAME_RE`: позиций 108→104, находок 121→117 — измеримое, а не заявленное сокращение шума.

## Г.54 Пиксельный предфильтр против «реестры совпали → пропуск»
Вопрос пользователя: не проверять ли всё «параллельно», без приоритета level=0. Разбор: это вопрос не параллелизма, а ПОЛНОТЫ, и полный отказ от level=0 на тысячах листов ломает саму цель (раздел 0.0 — экономия времени инспектора). Компромисс: `visual_prefilter.py` — сравнение рендеров БЕЗ ИИ (сетка 16×16 яркостей через `pymupdf.Pixmap.pixel`, без новых зависимостей), промоутирует level=0-пару чертежей в очередь на зрение при заметной разнице картинок. Очередь выросла со 121 до 213 пар. **Честная поправка к собственному диагнозу**: пара с пом. 140/142 оказалась УЖЕ уровня 2 — Г.54 расширяет охват, но именно этот случай не решает.

## Г.55 Кроп зоны отличия — сработал честно, но обнажил другую границу (не код)
`diff_hot_zone()` возвращает координаты отличающихся клеток в долях листа, `vision.py` рендерит кроп по ним (`clip_frac`) — против насыщенного плана на сотню помещений, где локальное изменение тонет при сравнении целиком. На реальной паре (ПД стр. 85 ↔ РД стр. 18): `visual_diff_ratio`=0.39, но зона честно вернула `None` — отличие покрывает >75% листа, потому что это ДВА РАЗНЫХ ЖАНРА листа (принципиальная схема против поэтажного плана, Г.5/Г.51). Предфильтр не стал выдумывать кроп. **Вывод**: блокер здесь не код, а то, что в переданном комплекте ПД у этой дисциплины нет поэтажных планов вообще.

## Г.56 Форма 3 (Г.47) молчала на совершенном виде глагола — реальный пропуск
`_GENERAL_PREDICATE_RE` содержала «выполня\w*» — только несовершенный вид; обычная форма технического требования («выполнить», «выполнены») не матчилась ни разу. Реальное предложение ПД вида «изделие ... **выполнить** из такого-то материала» — единственная текстовая зацепка для целой группы помещений — проходило мимо каталога из-за одного символа. Исправлено: корень сужен до `выполн\w*` (строгое надмножество прежнего), 88 требований вместо 84 на том же ПЗ.

## Г.57 Требование без номера — привязка к помещению по слову в названии
`match_requirement_rooms_by_name()` — сравнивает значимые слова (≥5 букв) требования со словами в НАЗВАНИИ помещения по общему 4-буквенному префиксу («химического»/«химии» расходятся на 5-й букве, более жёсткий порог не нашёл бы саму пару, ради которой правило заводится). Подсказка для инспектора, не строгая привязка и не вход в автосверку. **Первый прогон тут же поймал реальный шум** (Г.11 в действии): «кабинетах» цепляло 18 из 26 помещений по родовому слову — добавлен стоп-список префиксов (`каби`), после фикса 9 помещений, все по существу.

## Г.58 «Таблица ПД назначает помещению сущность» — механизм найден вручную, встроен в код
Источник — расчётная таблица ПД: она даёт по помещению не факт наличия системы, а КОНКРЕТНУЮ систему и её именованные ответвления. Текстовый слой и в таблице, и в подписях ответвлений на плане РД пуст (та же природа, что Г.44) — deterministic-путь недоступен, нужно зрение с обеих сторон. Реализовано вызовами ИИ на страницу с трёхкатегорийным исходом: система не та / ответвление перенесено / ответвление отсутствует. **Позже обобщено (Г.88)**: модуль был написан под один раздел целиком и заменён на `room_entity_check.py`, где тип сущности приходит из реестра таблиц. **Честная граница**: ветки на плане физически повторяются в нескольких местах (подпись трассы в каждой точке поворота) — точная сверка требует ОДНОГО связного вызова зрения на весь лист, а не серии ручных кропов.

## Г.59 Механизм Г.58 обобщён — и обобщение сразу поймало реальный шум
Распознавание страницы искало ДОСЛОВНУЮ фразу заголовка — соглашение ОДНОГО автора, а не термин. Сужено до нормативного корня самого расчёта: термин обязателен для этого расчёта в любом проекте такого раздела, а формулировка заголовка — нет (тот же приём обобщения, что Г.36/Г.47). **Обобщение тут же поймало реальный шум**: корень матчил ещё 4 страницы того же тома (оглавление и три страницы прозы). Различитель — тот же, что Г.8 описал для этого класса: лист таблицы в кривых даёт короткий текст (~460 симв., только штамп), содержание и проза — на порядок длиннее. Добавлен порог 800 симв.: было 3 ложных из 7, стало ровно 3 из 3. Статус n=1 (Г.21) — термин обоснован стандартом, на втором комплекте не проверен.

## Г.60 «Нет в rooms» было неоднозначно — молчание вместо видимого пропуска
Помещение отсутствует в расчётной таблице СТРОКОЙ — нумерация в таблице перескакивает через него; найдено визуально и подтверждено кодом независимо. Это вскрыло неоднозначность в самом механизме: старый промпт просил ПРОПУСКАТЬ в `rooms` помещения без М.О., то есть «нет в `rooms`» означало одновременно «строка есть, столбец пуст» (легитимная тишина) и «строки нет вообще» (то, что Г.10 запрещает молчать). Исправлено: `extract_mo_table_page` возвращает ещё и `rooms_seen` (ВСЕ номера на листе), `find_uncovered_rooms()` даёт те, которых нет НИ СТРОКОЙ — отдельной видимой строкой отчёта.

## Г.61 Свод независимых сигналов в единый вердикт (идея пользователя)
Идея пользователя: независимые источники сканируют своё, а «отдельный ассистент делает выводы на основе других выводов». Не новая архитектура — `triangulation.py` уже считает источники МЕХАНИЧЕСКИ; не хватало естественно-языкового свода. Реализовано: `verdict_synthesis.py` — ОДИН вызов ИИ на ключ, без картинок, по тексту уже посчитанных находок (цена — число ключей с находками, не число страниц). Вердикт `нарушение|недостаточно_данных|не_является_нарушением`; сбой одного ключа не роняет прогон. **Ловушка, найденная при проектировании**: `Confirmation.sources`/`.details` НЕ выровнены поэлементно (первое уникально, второе — без дедупликации) — сопоставление по индексу дало бы случайные пары; `verdict_synthesis` работает от сырых `Signal`, минуя её.

## Г.62 Аудит осиротевших модулей `packages/backend/app` — 9 удалены, 1 подключён
Запрос пользователя: «возможно очень много модулей лишние». Механический аудит (grep реальных вызовов, не упоминаний в докстрингах) по 44 модулям нашёл 10, ни разу не вызванных ни из `main.py`, ни из CLI. Сначала отклонено ложное подозрение: `packages/api` — НЕ дубликат `packages/backend`, а намеренно отдельный второй сервер (Г.1). Удалено 9 модулей + 9 тестовых файлов по поштучному решению пользователя (в т.ч. вся цепочка «якорь для листов без экспликации»); `composition_registry.py` (Г.17) не удалён, а подключён как `--kind composition`. **Побочный баг, найденный этим подключением**: диспетчер `main()` направлял любой незнакомый `--kind` в ветку, ожидающую запись в `_KIND_ATTR` — `--kind mo` был сломан с самого Г.58 и никогда не проверялся через настоящий CLI. Первые тесты на CLI-диспетчер вообще.

## Г.63 Разделы за пределами ОВ — что реально в комплекте, что пока план
Пользователь: инструменты написаны под ОВ, нужны для всех разделов. Реальная ведомость «Состав документации» дала 19 марок этого объекта против 18 обобщённых кодов ГОСТ Р 21.1101 — добавлены наружные/внутренние сети, ИТП, слаботочка, АУПТ, ВТ (статус n=1, извлечено, не угадано). **Граница присланной пользователем сводки**: это нормативный обзор, не факт из документа — применено механически безопасное (расширение списка кодов), НЕ применено написание раздело-специфичных экстракторов для ВК/ЭОМ/КЖ без единого реального листа (урок Г.42). **Раздело-независимо уже сейчас**: реестры помещений/оборудования, требования и форма 3, маршрутизация, комплектность, свод вердиктов. Раздело-специфично только `ventilation_mo.py` и гейт `discipline_code=="ОВ"` в `matching.py`.

## Г.64 «Недостаточно данных» подавалось как тупик, а не как открытый вопрос
Вопрос пользователя: «где недостаточно данных является основанием для внимания инспектора». `synthesize_verdict` вызывается ТОЛЬКО на ключи, где уже есть настоящий сигнал — значит этот вердикт никогда не означает «ничего не нашли», а всегда «один источник нашёл аномалию, второй не подтвердил». Исправлено без смены контракта: промпт требует закончить `reasoning` конкретным действием, рендер печатает «ТРЕБУЕТ ПРОВЕРКИ ИНСПЕКТОРОМ». **Второй, более серьёзный пробел того же класса**: `build_tickets()` пропускает `confirmed`-ключи, но на реальном комплекте нашлось 3 ключа, где свод честно понизил `confirmed` до `недостаточно_данных` — такой ключ не получал пакет эскалации ВООБЩЕ. Добавлен досчёт пересечения `confirmed ∩ недостаточно_данных` отдельным блоком.

## Г.65 ventilation_mo подключён к --kind all
Пробел Г.64: `ventilation_mo` (в отличие от `routing_diff`, Г.50) в `run_triangulated` не встраивался вообще — без него `mo_table`-сигналы не появлялись в общем прогоне. Логика вынесена в `_run_mo_cross_check()`, обёртки — явный `--kind mo` и блок внутри `run_triangulated` на ТОМ ЖЕ списке помещений, что уже выбран для routing. **Реальный баг, найденный своим же тестом при подключении**: `find_uncovered_rooms()` вызывалась даже когда таблица в комплекте вообще не найдена — каждое запрошенное помещение получало сигнал «не найдено в таблице», хотя смысл Г.60 — «в НАЙДЕННОЙ таблице строки нет». Исправлено гейтом `table_pages_found > 0`.

## Г.66 Общий реестр типов таблиц — прямой вопрос пользователя про обобщённость
Вопрос в лоб: классификация таблиц заточена под один раздел или узнаёт таблицы других? Честный ответ был «под один» (единственный regex-корень). Построен `table_registry.py`: 11 типов таблиц, у каждого свой нормативный термин, общая `classify_table_page()` возвращает КАКОЙ это тип. Сами термины — данные реестра, а не механики: новый раздел подключается записью. **Честная граница в докстринге модуля**: статус n=1 только у одного типа; остальные 10 — заготовка n=0, термин обоснован нормативно, но не проверен ни на одном документе, и у них НЕТ извлечения данных — модуль умеет только узнать страницу, не читать её. **Баг, найденный своим тестом**: один из корней не учитывал беглую гласную и не матчил реальную словоформу — сужен (приём Г.56).

## Г.67 Первая полная проверка на полном комплекте всех разделов (58 файлов) — 4 точечные правки
Пользователь дал доступ к полному комплекту всех разделов через отдельную ЛОКАЛЬНУЮ сессию (файлы слишком тяжёлые для git). Отчёт собран без автоматического внедрения — решение по каждому пункту за пользователем. Г.12 соблюдён: отчёт обсуждён, но не закоммичен. Подтверждены 4 правки: (1) `water_balance` — 0 верных попаданий из 16, добавлен `TableKind.max_text_len` и сужение regex до совместного упоминания термина И слова «баланс»; (2) `escalation.py` — бинарная метка «room иначе оборудование» неверно подписывала находки комплектности, заменена словарём `_DOMAIN_LABELS`; (3) `_DESIGNATION_RE` — строка субподрядчика по чужой схеме шифров молча терялась; (4) `_DESIGNATION_SEARCH_RE` — паспортные данные «220V/1PH/50HZ» неотличимы от обозначения комплекта, добавлено «первый символ — буква» + лукбихайнд. Плюс дедупликация формы 3 (одинаковые формулировки с разных страниц — одна строка со счётчиком) и пометка `[только ссылка на норму]` (пометка, НЕ фильтр — Г.10). Архитектурные пункты отложены пользователем: требуют ещё одного реального раздела.

## Г.68 Проверка Г.67 на том же комплекте — до/после, один найденный побочный баг
Отдельная локальная сессия прогнала те же проверки ДО/ПОСЛЕ: `water_balance` — было 16 срабатываний / 0 настоящих таблиц, стало 2 из 2, 0 ложных; метка домена и дедупликация формы 3 — подтверждены; `_DESIGNATION_SEARCH_RE` — частично (класс «начинается с цифры» исчез, буквенно-цифровые модели оборудования через «/» остались, **решено не чинить** без большего числа примеров). **Реальный побочный баг, введённый самим Г.67**: PDF переносит длинное обозначение посреди строки, а расширенный класс сегмента включил «-» как обычный символ — усечённый фрагмент «...695-» стал валидным совпадением и регистрировался с ОБРЕЗАННЫМ обозначением, то есть неверными данными вместо честного пропуска. Исправлено лукбихайндом (совпадение не может кончаться пробелом/точкой/дефисом). Сшивание PDF-переносов — отдельная задача.

## Г.69 ЛЛМ-фильтр каталога формы 3 — второй проход поверх regex-кандидатов
Механизм предложен пользователем: regex находит кандидатов, потом они уходят вызовом ЛЛМ, которая решает по каждому — требование или шум. Обоснование пользователя: текстовые требования ПД — первичный вход, на котором строится вся остальная документация, поэтому качество каталога важнее его размера. `requirement_llm_filter.py`: пачками по 20 (на реальном комплекте 176-195 формулировок на раздел — вызов на каждую был бы дорог), ответ по ИНДЕКСУ, не по тексту (надёжнее при похожих формулировках). Отсеянное печатается отдельным видимым списком с обоснованием модели (Г.10 — ничего не выбрасывается молча). Раздело-независим по конструкции: модель судит по смыслу, не по коду раздела.

## Г.70 Критерий ЛЛМ-фильтра сужал требование до приказа — поправка пользователя
Пользователь указал на риск в первой версии критерия («обязывает что-то сделать»): реальное нарушение №2 устроено так, что текст НЕ приказывает, а констатирует («предусмотрена система подогрева полов») — строгий критерий отсеял бы ровно ту точку сравнения, ради которой форма 3 существует. Критерий переопределён: не грамматическое наклонение, а СОДЕРЖАНИЕ — есть ли конкретный проверяемый технический факт, сравнимый с РД. Констатация факта и описание способа исполнения — тоже требование. Конкретный пример «тёплые полы» в промпт НЕ вставлен: Г.11/Г.12 запрещают учить модель искать категорию текущего проверяемого объекта.

## Г.71 Сокращён набор провайдеров ЛЛМ до Anthropic + GigaChat
Решение пользователя: «убери лишние ллм, оставь только себя и гигачат». Из 6 провайдеров ни один из local/OpenAI/Google/YandexGPT реально не использовался за всю историю проекта. Дефолт `packages/backend` переключён с `"local"` на `"anthropic"` — иначе первый же вызов падал бы `ValueError: unknown provider`. **Не тронуто осознанно**: `packages/llm_core/` старого движка — у неё СВОЯ политика суверенности (`config/providers.yaml`, `claude` там explicit `enabled: false` с пометкой «не отечественное ПО»); менять её означало бы менять заявленную комплаенс-политику продукта, а не убирать неиспользуемый код.

## Г.72 Провайдер-трим Г.71 не дошёл до WSL-скриптов и документации
Пользователь запустил `START-NADZOR.bat` и получил «Локальная модель Ollama ✓ qwen2.5vl на месте» — прямое противоречие Г.71. Г.71 правил только `app/llm.py` и прямых потребителей; shell-скрипты остались, потому что `provider="local"` физически уже не мог сработать, но старую Ollama-детекцию это не отключало — скрипт искал её вслепую, вводя в заблуждение об архитектуре. Исправлено: `setup.sh`, `start-all.sh`, оба README, комментарий `VISION_MAX_DIM` в `vision.py`. **Не тронуто**: `docker-compose.yml` (профиль относится к суверенной политике старого движка, не к Ollama здесь) и исторические записи Г.3/Г.38 — append-only, не переписываются задним числом.

## Г.73 Прогон по всем разделам одной командой + реальный краш, найденный смоук-тестом
Жалоба пользователя: конкурс через две недели, а CLI требует вручную подбирать пути одного раздела за раз. `run_all_disciplines.py` — рекурсивно находит `*.pdf`, классифицирует КАЖДЫЙ файл той же `classify_document` (не эвристикой по имени папки), группирует по разделам, вызывает `run_triangulated` (ПД+РД), `run_requirements` (только ПД) или честно пропускает (только РД). **Реальный краш, найденный своим же смоук-тестом**: `extract_mo_table_page` — единственное место среди всех ЛЛМ-вызовов проекта БЕЗ try/except; с многораздельным раннером один упавший вызов рушил бы весь пакетный прогон. Исправлено там же, плюс try/except на каждый раздел как защита в глубину.

## Г.74 ЛЛМ-фильтр не отфильтровал НИ ОДНОГО кандидата на реальном ключе
Реальный прогон с ключом GigaChat на 4 разделах: из 347 regex-кандидатов фильтр оставил ВСЕ 347, включая явные декларации, которые по критерию промпта обязан был отсеять. Отчёт предположил «баг качества модели или промпта». Проверка кода (Г.11 — не соглашаться с правдоподобным диагнозом без проверки) нашла более вероятную причину в разборе ответа: `bool(item.get("is_requirement", True))` — GigaChat не даёт строгой JSON-схемы (Г.39), и если модель вернула булево СТРОКОЙ `"false"`, то `bool("false")` в Python равно `True`, то есть каждый отрицательный вердикт молча превращался в положительный ДО рендера. Исправлено `_parse_verdict_bool()`. **Гипотеза не подтверждена живым вызовом** — у этой сессии нет ключа (Г.37/39).

## Г.75 «Обучить ГигаЧат под сервис» — фильтр не был подключён к самому сервису
Вопрос пользователя: «сделать так, чтобы гигачат был обучен под то, что мы делаем, чтобы в сервисе он корректно работал». Уточнение: дообучение весов здесь недоступно (нет договора/датасета/API) — вся адаптация это промпт и few-shot (Г.37), и к багу Г.74 (парсинг) обучение отношения не имеет. Но за формулировкой стояла РЕАЛЬНАЯ проблема: `triangulated_pipeline.py` — HTTP-путь `POST /triangulated-runs`, вызываемый фронтендом, то есть САМ СЕРВИС — извлекал форму 3 регуляркой и НИКОГДА не прогонял её через ЛЛМ-фильтр, даже когда ключ уже используется соседними шагами; а `not_run` об этом пробеле молчал (само по себе нарушение Г.10). Исправлено, результат — в `requirements.general.llm_filter`.

## Г.76 Гипотеза Г.74 (строка vs bool) оказалась недостаточной — причина в критерии промпта
Повторный реальный прогон с фиксом Г.74: результат идентичен, 0 отсеяно из 236. **Честно: гипотеза Г.74 не подтвердилась как единственная причина** — фикс остаётся правильным кодом, но наблюдаемый 100%-ный неотсев не объясняет. Пользователь сам локализовал природу: «любые способы ведения работ он воспринимает как требования вдобавок» — критерий Г.70 («описывает способ исполнения — тоже требование») сформулирован без верхней границы. Два нераспознанных класса шума из реального прогона: мета-утверждения об объёме РАЗДЕЛА ДОКУМЕНТА («...нет необходимости» — сравнивать в РД нечего) и классификационные утверждения о категории объекта. Промпт сужен до ПОЛОЖИТЕЛЬНОГО факта про объект, оба класса добавлены явно, пример — синтетика, не цитата с проверяемого объекта (Г.11/Г.12).

## Г.77 Третья итерация: 0 отсеяно СНОВА — причина не в промпте, а в скрытом сбое связи
Третий подряд заход с идентичным результатом (236/236) — статистически это уже не «модель не понимает критерий»: три РАЗНЫХ формулировки дали бы хоть какую-то вариативность. В том же отчёте прямая улика: `[SSL: CERTIFICATE_VERIFY_FAILED] self-signed certificate in certificate chain` на КАЖДОМ из 17 вызовов семантической сверки — ровно сценарий, предвиденный Г.39 («может понадобиться `GIGACHAT_CA_BUNDLE=false` на машине без НУЦ Минцифры в доверенном наборе ОС»). Сбой вызова молча превращал каждый вердикт в «оставить» через `except: result=None`. **Урок Г.10 в чистом виде**: 100%-ный отказ связи был неотличим от «модель со всем согласна». Добавлены `on_chunk_error`/`on_batch_error` — сбой печатается сразу, а не превращается в двусмысленный результат.

## Г.78 Диагноз Г.77 подтверждён: SSL был реальной причиной; новый сбой — rate-limit
Пользователь добавил `GIGACHAT_CA_BUNDLE=false` и перепрогнал те же 4 раздела: SSL-ошибка исчезла (ОВ 5037→0, АР 1156→0), **ЛЛМ-фильтр реально отсеивает шум** — ОВ 11 из 88, АР 13 из 75, все 4 целевых предложения Г.74-77 отсеяны с адекватным обоснованием модели. Промпт работал как задумано; три предыдущих раунда тестировались вслепую на неработавшей связи. КР/НВ получили НОВЫЙ сбой — `429 Too Many Requests`: `requirement_text_verify` (Г.49) делает вызов на КАЖДУЮ страницу РД для КАЖДОГО требования — сотни-полторы тысячи вызовов подряд. Исправлено в `_post_json` (единая точка ВСЕХ вызовов обоих провайдеров): до 3 повторов с задержкой (`Retry-After` или экспонента) вместо немедленного отказа.

## Г.79 Идентификация по номеру тома — код «ООС» не распознавался
Переход на работу по одному тому за раз: реальные файлы одного из разделов найдены ручным поиском, не через `classify_document()` — составного кода `ООС` не было в `DISCIPLINE_CODES` (там был буквенный `ОС` по ГОСТ Р 21.1101, но это два разных обозначения одного раздела в двух нормативных системах, реально встречаются оба — тот же класс наблюдения, что Г.63 фиксировал для НВ/ВВ/ИТП). Добавлен. Статус n=1 (Г.21): один комплект, оба файла — один объект.

## Г.80 Тома без буквенного кода вообще; схема работы зря искала РД
Реальный список файлов: имя содержит только номер тома, буквенного кода раздела в нём НЕТ вовсе — программа не видела такие тома. Разбор маркировки вида «NN-РР-NN-NN-NN» показал: второе число — номер раздела по Постановлению №87, подтверждено на всех 4 файлах сразу. Реализовано `_section_number_code()` — резервный сигнал ПОСЛЕ буквенного сканирования, только для разделов, однозначно не делящихся на дисциплины внутри себя: ГП(2)/АР(3)/КР(4)/ПОС(6)/ООС(8)/ПБ(9); раздел 5 сознательно исключён (делится на ОВ/ВК/ЭОМ). Новый источник `filename_section_number`, отличимый от прямого совпадения. **Отдельная находка того же захода**: `--kind requirements --verify-requirements` всё равно прогонял шаги сверки с РД, даже когда `--after` тот же файл, что `--before` — реального РД на этой стадии нет, а именно эти шаги жрут основную часть лимита запросов. Создан `scripts/summarize_requirements.py` — только извлечение, без единого шага, зависящего от РД.

## Г.81 Порядок вывода тоже расставлял приоритеты неверно
Пользователь: вывод «настроен на приоритет с извлечения помещений», хотя нужны в первую очередь требования. По факту скрипт печатал обзор тома («помещений: N, позиций оборудования: N») ПЕРВОЙ строкой — счётчики к самому скрипту отношения не имеют (он ни разу не смотрит на реестр помещений), но выглядели главным результатом. Переставлено: требования первыми, справка о файле последней, и это укороченная строка `_identity_line()` без счётчиков вообще.

## Г.82 Независимый аудит (Opus) правок Г.71-81 — код подтверждён, 2 новых критичных бага в инструкции
После потерянного дня пользователь заказал независимую проверку с явным условием НЕ доверять CLAUDE.md на слово. Все 5 проверенных пунктов подтвердились реальным чтением кода и прогонами (386/386). Но аудит нашёл то, ради чего запускался — два бага ровно того класса, что уже стоил дня (инструкция звучит правдоподобно, поведение кода другое): (1) `summarize_requirements.py` при несуществующем `--pd` давал чистый отчёт с нулями и `EXIT=0`, неотличимый от «в томе нет требований» — теперь падает явной ошибкой ДО обращения к ЛЛМ; (2) тот же скрипт не читал `os.environ` вообще, из-за чего `GIGACHAT_CREDENTIALS`/`GIGACHAT_CA_BUNDLE` из `vision-keys.env` не действовали — прямой путь повторить SSL-тупик Г.77; добавлен env-фолбэк как в `registry_diff.py`. Плюс: `on_batch_error` подключён в CLI, потолок 30с на `Retry-After` (провайдер мог усыпить прогон на часы молча), `_SECTION_NUMBER_RE` сужена требованием слова «Том» рядом (ловила номер договора как код раздела).

## Г.83 Приоритет внутри извлечения был обратным; «извлечено: 0» диагностировано
Указание пользователя: «поменяй приоритет на извлечение требований в тексте — из-за количества пунктов часть не работает как надо, ведь любая программа работает последовательно». Наблюдение подтвердилось измерением: на том же 177-страничном томе ОВ форма 1/2 — **69 вызовов ЛЛМ, форма 3 — 5**, то есть шаг, дающий на этом этапе ноль, забирал 93% бюджета и стоял ПЕРВЫМ, выбирая лимит частоты до того, как очередь доходила до нужного (объяснение 429 на КР/НВ со стороны самого извлечения, в дополнение к Г.49). Форма 3 теперь первая и по умолчанию единственная; форма 1/2 — опция `--with-room-requirements` с явной строкой «НЕ запускалась» и причиной. **Заодно закрыта находка Г.78 «извлечено: 0»**: причина не сетевая — `requirement_llm_extract` отбрасывает `if not rooms: continue` каждое требование без номера помещения, молча. Форма 1/2 по контракту питает автосверку по номеру (Г.33/46), для неё это правильно; неправильным было молчание (Г.10). На разделах, где требования относятся к объекту целиком (ООС, ПОС, ПБ), так выглядел ЛЮБОЙ прогон при исправной связи. Добавлен `on_skipped_no_rooms`. **`CONTEXT.md`** — новый файл-ориентир: почему пунктов много (журнал находок, не проект), разбивка всех 83 по этапам с явным «применимо сейчас / нет» (к текущему этапу — 24 из 83), грабли, карта файлов.

## Г.84 Чтение штампа зрением роняло весь прогон — уцелевший экземпляр бага Г.73
Проверка программы по прямой просьбе пользователя. За `vision_stamp_fn` (`classify_document`) стоит цепочка `make_llm_stamp_classifier` → `read_stamp_by_vision` → `call_llm_json` БЕЗ try/except, и `scan_cli.py::_load_side` защиты у себя тоже не имел: любой сбой вызова (истёкший ключ, 429, SSL — всё наблюдалось живьём, Г.77/78) ронял ВЕСЬ прогон на первом же документе с растровым штампом вместо честного «раздел не определён» — того самого поведения, что штатно даёт режим `--no-llm`. Ровно тот класс, что Г.73 нашёл и починил в `ventilation_mo.py`; этот экземпляр уцелел. Исправлено в единой точке — внутри `classify_document`, а не у каждого вызывающего (`main.py` имел свою защиту, `scan_cli.py` — нет): сбой ловится, печатается на stderr и попадает в новое поле `ClassificationResult.vision_error`, чтобы «раздел не определён» и «раздел не прочитан из-за сбоя связи» не выглядели одинаково (Г.10, тот же приём, что поле `error` у `extract_mo_table_page`). Проверено реальной цепочкой с нерабочим ключом, не только тестом: прогон доходит до конца, ошибка видна. 2 новых теста, 398/398.

## Г.85 Старый бэкенд удалён целиком — двух движков больше нет
Прямое решение пользователя: «удали старый бекенд вообще». `packages/backend` не зависел от него ни одной строкой — проверено ДО удаления, тесты не дрогнули (398/398). Удалены `packages/api`+`analysis`+`documents`+`integrations`+`llm_core`+`norms`+`audit`, их тесты, `db/`, `config/`, `.importlinter`, `docs/architecture.md`/`metrics.md` и 14 экранов витрины вместе с авторизацией: 152 файла, −12 527 строк. **Г.1 отменён этой записью**: бэкенд теперь один, «проверить, нет ли реализации в другом» больше не про что.

## Г.86 Стадия ПД стала самостоятельной; ЛЛМ читает текст напрямую
Разворот по указаниям пользователя: разбор ПД — основной продукт, сверка с РД — надстройка. **Отсутствие РД больше не режим ошибки, а норма** («её нет и не будет на половине документов») — ни один шаг стадии ПД от РД не зависит. Порядок цепочки исправлен на комплект→факты→требования→сводка→ЛЛМ-фильтр→реестр→графика. Извлечение: модель читает текст страниц и возвращает ГОТОВЫЕ требования с разделом и страницей — без regex-отбора кандидатов и без второго прохода на отсев шума («можно просто весь текст целиком отправлять в ЛЛМ, чтобы она сразу делала сводку»). Цена принята: ~69 вызовов против ~5 у regex, взамен уходит слепое пятно регулярки (реальный пропуск Г.56). Regex остаётся запасным путём без ключа — программа не встаёт, сводка выдаётся сырой.

## Г.87 Одно хранилище на передачу между стадиями и на датасет
`pd_store.py` (`data/pd_store.db`, намеренно НЕ `nadzor.db` — её удаляет `make clean`): `compare_with_rd.py` читает СОХРАНЁННЫЙ разбор, а не переизвлекает ПД заново (разбор тома — десятки вызовов ЛЛМ). Тот же ряд — датасет на будущее: у каждого требования копится файл, раздел, страница, извлекатель (llm/regex), провайдер, модель, версия промпта, число упавших пачек — **без этих полей датасет нельзя отфильтровать по качеству**, а неполный прогон нельзя выдать за полный разбор тома. Поиск прогонов идёт по `PdRun.documents`, а не по строкам требований: иначе том с нулём требований выглядел бы «никогда не разбиравшимся» (подстановка «пусто» вместо «не делали», Г.10).

## Г.88 Код был заточен под один пример — механизмы обобщены, наблюдения вынесены в данные
Прямое и категоричное указание пользователя: «убери из кода акцент на конкретный пример, это была ошибка. КОД ДОЛЖЕН БЫТЬ ОБЩИМ». Аудит исполняемого кода (не докстрингов — разбором AST, чтобы отделить логику от комментариев-обоснований) нашёл три реальных места, где под один раздел была заточена САМА ЛОГИКА, а не только история находки. (1) `matching.py` гейтировал различение подсистем сравнением кода раздела со строковой константой, а `subsystem.py` держал два зашитых словаря — переписано на реестр `SUBSYSTEM_VOCABULARIES` (раздел → подсистемы → лексика, у каждой записи честный статус n=1/n=0 по Г.21); вызывающий код передаёт раздел внутрь и больше не сравнивает его со строковой константой у себя. (2) модуль сверки назначений был написан под ОДИН раздел целиком — заголовок таблицы, названия столбцов, вид обозначений и примеры сидели прямо в промптах. Заменён на `room_entity_check.py`: механизм «таблица ПД назначает помещению сущность → эта сущность обязана быть на листе РД у того же помещения» к разделу не привязан вообще (у каждого раздела своя сущность — своё именованное ответвление инженерной сети или конструктива, — но наблюдение одно и то же), поэтому промпты СОБИРАЮТСЯ из описания типа таблицы. (3) `table_registry.TableKind` получил поля `entity_name`/`entity_examples`/`system_column`/`entity_column` — новый раздел подключается заполнением данных, без единой правки кода сверки. Ключевой тест обобщения проверяет не подстановку полей, а результат: те же три типа расхождений находятся на ВК — разделе, под который в коде не написано ни строки; отдельно проверено, что лексика ОВ не течёт в промпт чужого раздела. 413/413.

## Г.89 Обобщение доведено до комментариев; страница длиннее бюджета режется
Поправка пользователя к Г.88: «надо не разделять и применить к общим правилам, чтобы не было опять акцента на ОВ» — деление на «исполняемый код чиню, комментарии оставляю как историю» отклонено. Комментарий-обоснование задаёт рамку следующему читателю не слабее кода, а промпт с примерами одного раздела прямо якорит модель на чужом документе. Пройдено 19 модулей: журнал замеров `routing_graph.py` переписан ролями («подпись ветки», «подпись точки сбора») с сохранением всех чисел; примеры в промптах `stamp_vision.py` и перечень источников в `verdict_synthesis.py` (называл удалённый ОВ-модуль) обобщены; шифр реального объекта в комментариях `classification.py`/`composition_registry.py`/`equipment.py` обезличен (Г.12/Г.24 — форма шифра и есть предмет объяснения, идентификатор нет). **Четвёртая заточка кода, которую AST-аудит Г.88 пропустил**: `TARGET_CODE_RE` держал раздел внутри самого шаблона (`М\.?\s?О\.?`) — обобщён до произвольного буквенного префикса типа элемента. Остались только реестровые данные (`table_registry`/`subsystem`/`set_overview`) — там им и место, и фикстуры тестов, где имена реальных файлов функциональны, а не иллюстративны.
Вторая половина той же поправки — **разбиение большого документа**: потолок символов на вызов проверялся только на СУММЕ пачки, поэтому одиночная страница сверх бюджета уезжала в модель целиком. Замер на реальном томе: страница 6051 символов при бюджете 6000 → одна пачка сверх лимита. `_split_long_page()` режет по границам предложений (жёсткий срез — только если границ нет вовсе), метка страницы называет часть («--- Страница N (часть k из m) ---»), иначе модель видит два одинаковых номера. После: 57 пачек, максимум 5955, сверх бюджета 0. 403 теста.

## Г.90 Требование с нескольких страниц — одна строка со списком страниц
Найдено первым же прогоном обновлённой программы на реальном томе. Примечание, повторённое на каждом листе многостраничной таблицы, печаталось инспектору отдельной строкой на КАЖДУЮ страницу: 88 строк против 69 уникальных формулировок, одна фраза шесть раз подряд. Дедупликация с перечислением всех страниц уже существовала в каталоге формы 3 (Г.67), но при перестройке сводки под инспектора (Г.86) в неё не перенеслась — регрессия, внесённая самой перестройкой. Подтверждено пользователем как правило: «это нормально, что требования на нескольких страницах; надо, чтобы программа тогда на несколько и ссылалась». Одна строка, `стр.12, 99, 100, 101, 102, 103`, явный счётчик `(повторено 6×)` и число уникальных формулировок в заголовке — **сжимается вид, а не данные** (Г.10): ни одна страница не теряется. Ключ склейки — нормализованный текст (`_normalize_for_dedup`, общий с Г.67, чтобы «одинаковость» не разошлась в двух отчётах); границу документа склейка НЕ переходит — одна фраза в двух томах это два факта, инспектору важно, в каком томе искать. Раздело-независимо по конструкции: в правиле нет ни лексики, ни кода раздела. Вывод сократился со 199 строк до 161. 3 теста, 406 всего.

## Г.91 Предполётная проверка связи — прогон больше не уходит в сеть вслепую
Пользователь передал рабочий ключ GigaChat и попросил вызвать его из облачной сессии. Вызвать не удалось, и причина не в ключе и не в коде: сеть этой среды режет все хосты GigaChat. Диагноз доведён до конца, а не остановлен на «не работает»: `api.giga.chat:443`, `gigachat.devices.sberbank.ru:443`, `developers.sber.ru:443` — **403 на CONNECT** (политика egress); `ngw.devices.sberbank.ru:9443` — CONNECT проходит (200 Connection Established), но на ClientHello в 517 байт приходит сброс, то есть режется на выходе, просто иначе. Обходить политику нельзя и не нужно. **Решение на стороне среды**: в настройках cloud-окружения (claude.ai/code → значок окружения) выставить Network access = **Custom** и внести `ngw.devices.sberbank.ru`, `api.giga.chat`, `gigachat.devices.sberbank.ru`, отметив «включить список по умолчанию». Историческая поправка: по журналу (Г.39, Г.78) ВСЕ живые вызовы GigaChat шли с машины пользователя; из облачной сессии успешного вызова не было ни разу — впечатление обратного проверено и не подтвердилось.
**Что сделано кодом** (единственное, что здесь чинится): `check_llm_reachable()` — один короткий вызов ДО начала разбора. Разбор тома это десятки вызовов и минуты; при обрыве связи каждый молча возвращал пустой результат, и прогон заканчивался правдоподобным отчётом, где ничего не найдено, — ровно ловушка Г.77, стоившая трёх раундов правок промпта против бага, которого в промпте не было. Теперь: `--check-llm` проверяет связь и выходит (код 0/2), а обычный прогон с ключом при неудаче **останавливается с явной ошибкой** и не выдаёт пустой результат за разбор. Три состояния различаются текстом, а не сводятся к «не получилось»: ключа нет / связь есть / связь не прошла с точной причиной — «сеть не пускает» и «ключ протух» требуют от пользователя разного. Проверено на живом отказе: `ConnectError: [Errno 104] Connection reset by peer`, коды 2 и 1. 3 теста, 409 всего.

## Г.92 Первый прогон ЛЛМ-пути на реальном томе — вместо провайдера сыграла сессия
GigaChat из облака недоступен (Г.91), поэтому по указанию пользователя роль модели сыграла сама сессия: настоящий код, те же 69 пачек, тот же промпт, тот же разбор ответа и то же хранилище — подменён ровно один вызов `call_llm_json`. Прогон помечен в хранилище провайдером `claude-in-session`, а НЕ `gigachat`: датасет (Г.87) обязан отличать, чем получена строка, иначе фильтрация по качеству бессмысленна. **Результат против regex-пути на том же томе: 308 требований против 88, с номерами помещений 73 против 2, с кодом системы 192 против 0, страниц-источников 65 против 24.** Три класса шума, которые regex выписывал как требования, отсеяны полностью: строки оглавления тома (3 шт.), утверждения о самом документе «нет необходимости» (4 шт.), классификация объекта (1 шт.) — те самые классы, которые Г.70/Г.76 формулировали вслепую при неработавшей связи. Найдено то, чего regex не видел вовсе: 46 систем с номерами обслуживаемых помещений на двух листах, расчётные параметры по каждой системе, марки установок из ведомости и параметры с напорами — то есть именно те таблично изложенные факты, ради которых сводка и нужна. **Честно о своих ошибках как модели**: сравнение с regex-путём вскрыло один реальный пропуск — принципиальную схему я закрыл как «подписи без смысла», а там был размерный параметр изделия; regex его вытащил. Вывод: regex-путь ценен не только как запасной без ключа, но и как перекрёстная проверка ЛЛМ-пути. **Чего этот прогон НЕ проверяет**: качество GigaChat. Модель здесь другая и сильнее; переносить эти цифры на GigaChat нельзя (Г.11), но конвейер, разбивка на пачки, привязка к разделу/странице, дедупликация Г.90 и сохранение прогона проверены на настоящих данных от начала до конца.

## Г.93 Сводка была копией документа: выжимка вычислялась и выбрасывалась
Замечание пользователя по реальному прогону Г.92: «порой кажется, что целые листы прям взял и скопировал каждый абзац; надо всё-таки какую-то выжимку делать». Замечание точное, и причин оказалось ДВЕ. **Первая — в коде.** Промпт всегда требовал от модели два поля: `requirement` (суть) и `sentence` (дословная цитата). Разбор ответа брал суть ТОЛЬКО как запасной вариант при отсутствующей цитате (`item.get("sentence") or item.get("requirement")`), то есть при нормальном ответе выбрасывал её, а `render_summary` печатал цитату. Выжимка вычислялась моделью и уничтожалась разбором — инспектор получал переписанный документ. Исправлено: `Requirement.summary` хранится рядом с цитатой, сводка печатает суть (цитата остаётся доказательством и уходит в хранилище и в пакет эскалации), дедупликация Г.90 группирует по сути, а не по цитате — одно требование, процитированное с разной разбивкой строк на разных листах, теперь сходится в одну строку. Без короткой формы (regex-путь её не делает) показывается цитата: показать длинное лучше, чем не показать ничего (Г.10). **Вторая — в промпте**, и без неё первая почти ничего не даёт: формулировка «суть требования одним предложением» допускает пересказ, и модель (в прогоне Г.92 — я) его и писала: медиана 94, максимум 316 символов при цитате 113. Промпт переписан на жёсткое правило: не более 100 символов, называть проверяемый факт, а не форму изложения ПД («предусмотрено», «в соответствии с» — выбрасывать), перечисление длиннее трёх позиций сворачивать числом, параметр писать числом с единицей; добавлены пары «плохо/хорошо». **Замер на том же томе после обеих правок**: медиана выжимки 52 символа против 112 у цитаты, максимум 100, сводка сжалась с 64,5 КБ до 22 КБ при тех же 308 требованиях — сжат ВИД, ни одно требование не потеряно. 3 теста, 412 всего.

## Г.94 Кнопка для инспектора: стадия разбора ПД появилась на сервере
Указание пользователя: «инспектор загрузил документацию, нажал кнопку и система заработала сразу как надо, а не как мы каждый раз промт присылали». Проверка показала: **промпты давно зашиты в код** и никто их руками не присылает — но сервер стадию 1 (Г.86) не умел вообще. HTTP-эндпоинты были только для сравнения ПД↔РД, то есть требовали РД, которой на половине объектов нет; самостоятельный разбор ПД жил ТОЛЬКО в CLI, и через интерфейс сводку получить было нельзя. Причина структурная: рендер сводки и загрузка текста страниц лежали внутри `scripts/summarize_requirements.py`, а `scripts` — не пакет приложения, сервер их импортировать не может. Сделано: логика перенесена в `app/pd_stage.py` (`load_text_facts`, `render_summary`), предполётная проверка связи — в `app/llm.py`; CLI и HTTP стали двумя вызывающими ОДНОГО кода, а не двумя копиями, которые разойдутся. Новое: модель `PdRun`, `POST /pd-runs` (на входе только список документов — ни промпта, ни модели, ни ключа), `GET /pd-runs/{id}`, `GET /pd-runs`, `GET /llm-check`. Прогон фоновый, связь проверяется ДО разбора и при отказе прогон **останавливается с явной причиной**, а не отдаёт пустую сводку (Г.77/Г.91). Результат уходит в общее хранилище разборов (Г.87) — оттуда его берёт стадия сверки с РД. **Ключ: из настроек, а если пусто — из `GIGACHAT_CREDENTIALS`**, поэтому администратор задаёт его один раз при развёртывании, а инспектор не вводит ничего. Провайдер по умолчанию переключён с `anthropic` на `gigachat`. Фронт: карточка «Разбор проектной документации» с кнопкой, состоянием связи до запуска и сводкой; стоит ПЕРЕД сравнением с РД, как требует порядок Г.86. **Существующий тест поймал реальное следствие смены умолчания** — он мокал транспорт Anthropic и молча опирался на то, что это провайдер по умолчанию; допущение сделано явным. 6 новых тестов, 418 всего; фронт собирается, tsc чистый.

## Г.95 Разбор РД тем же механизмом + состав тома вместо пустой сводки
Указание пользователя: «делай отдельно разбор РД, чтобы также сводка была; если в РД нет текста для сводки, то просто он должен говорить, что в документе столько листов графической части, столько спецификаций и т.д.». `document_composition.py` (изначально назывался `rd_overview.py` — имя врало, код работает на ЛЮБОМ документе и прогонялся на ПД, переименован по поправке пользователя «код общий на любой вид документации»): листов всего, чертежей и текстовых, без текстового слоя, таблицы по типам из общего реестра, листов со связным текстом. Детерминированно, без ЛЛМ, за секунды. **Состав считается ВСЕГДА, а не только когда сводка пуста**: у рабочей документации текстового слоя почти нет по природе (Г.8/Г.59), и пустая сводка без состава неотличима от сбоя связи — та же подмена, что запрещает Г.10; отчёт прямо говорит, что связного текста нет и почему. Разбор РД — та же кнопка со `side='after'`, не вторая таблица: механизм один. Разница одна и документирована: разбор ПД сохраняется в хранилище для стадии сверки, разбор РД — нет, сверке нужен ТЕКСТ рабочей документации, а не извлечённые из неё требования. **Реальная находка на живых данных**: общий порог реестра таблиц в 800 символов отбрасывал лист спецификации целиком — замер на трёх реальных томах дал 1153, 1374 и 1509 символов, потому что заголовок таблицы и названия столбцов весят больше, чем лист в кривых, под который порог подбирался. Порог поднят до 1800: между максимумом замера и объёмом страницы прозы того же тома (2449-2651), то есть по измеренной границе, а не с запасом; тест закрывает обе стороны. **Две ошибки в собственных фикстурах**, найденные по дороге: `insert_text` не кодирует кириллицу (`get_text` возвращал точки вместо букв) и длинный текст уезжал за край листа — тесты проверяли не то, что написано в их названиях.

## Г.96 Третья кнопка: соответствие РД требованиям ПД, где «не подтверждено» ≠ «нарушение»
Указание пользователя вместе с его же предупреждением: «может быть много ложных срабатываний, ведь в РД меньше текста; если в ПД указано, что в таких-то местах делаем так, то система уже в РД смотрит по чертежам (если нет требования в тексте), так ли сделано». **Главное правило модуля — отсутствие подтверждения НИКОГДА не называется нарушением.** В рабочей документации связного текста мало по природе (Г.8): требование, записанное в ПД словами, в РД выражено линией на плане и строкой спецификации. Поэтому «не нашли в тексте» — ожидаемое состояние, а не улика, и статусов три, ни один из них не вердикт: `подтверждено` / `требует проверки` (с указанием листов, куда смотреть) / `не проверялось` (нечем: нет ключа, сбой, не за что зацепиться). Прямое следствие Б.6. Отчёт НАЧИНАЕТСЯ с оговорки, а не заканчивается ею: без неё 60 позиций «требует проверки» читаются как 60 нарушений. `app/compliance.py` — лестница от дешёвого к дорогому, каждая ступень работает только над нерешённым предыдущей: (1) токен нормы/марки в тексте РД без вызова модели (Г.48); (2) смысловая сверка текста РД моделью пачками (Г.49); (3) зрение по листам (Г.35) — ТОЛЬКО для требований с номерами помещений, иначе лист выбирать не по чему, и это честно помечается вместо слепого перебора. Сбой вызова даёт «не проверялось», а не «в РД нет» (Г.77). Требования берутся из СОХРАНЁННОГО разбора ПД, а не извлекаются заново (Г.87). `POST /compliance-runs` + карточка на фронте с оговоркой до цифр. **Поправка пользователя по ходу**: «РД имеет тот же принцип, что и ПД, код общий на любой вид документации» — модуль состава переименован `rd_overview.py` → `document_composition.py` (имя говорило «РД», а код работал на любом документе и прогонялся на ПД), карточка разбора на фронте стала ОДНИМ компонентом на обе стороны. 436 тестов, фронт собирается.

## Г.97 Автоопределение раздела осталось, но последнее слово за инспектором
Указание пользователя: «чтобы система не путалась — можно сохранить способы определения тома документации, но добавить опцию ручного выбора или ввода/корректировки тома, если файл не будет опознан». Автоматика (имя файла, титульный лист, штамп текстом и зрением, номер раздела по ПП №87 — Г.13/79/80) работает как раньше и ничего не потеряла. Добавлен `PATCH /documents/{id}`: инспектор выбирает раздел из справочника, источник в списке файлов честно показывает «указан вручную» — программа и человек различимы (Г.10). **Ручной раздел не просто метка**: он побеждает автоматику и ОТМЕНЯЕТ само определение в `pd_stage.load_text_facts` и `document_composition.describe_volume` — угадывание поверх ручного ввода было бы способом тихо его потерять; тест проверяет, что заданный раздел доезжает до разбора, а не остаётся в списке файлов. `discipline_code=null` снимает правку и ПЕРЕСЧИТЫВАЕТ автоматику заново, а не оставляет прежнее значение: иначе «отменил» тихо сохраняло бы результат отменённого. Неизвестный код отклоняется с перечнем допустимых — опечатка не сохраняется молча. Зачем это нужно по существу: не опознанный файл уходил в разбор с «раздел не определён», и требования оставались без привязки к разделу, а привязка — прямое требование пользователя из Г.86. 440 тестов.

## Г.98 Сводка покрывает весь документ; резерв по отметке этажа; состав считается без ключа
Три указания пользователя одним сообщением. **(1) Резервный подбор листов** по отметке этажа (Г.40) подключён к сверке: план и его экспликация лежат на РАЗНЫХ листах, и без резерва требование, чьё помещение не нашлось в текстовом слое РД, оставалось без единого кандидата на просмотр. **(2) Сводка обязана покрывать ВЕСЬ документ**: «если только по тексту выжимать, то неполноценности мы накодили; граф и другие материалы игнорятся, их можно в сводке отобразить, что на таких-то листах граф материал». Добавлен перечень графической части — лист → наименование чертежа из штампа текстом (Г.59: штамп остаётся текстом чаще содержимого), одинаковые наименования сворачиваются в одну строку со списком листов (приём Г.90), нечитаемые попадают туда же как «наименование не прочитано», а не исчезают. **Замер сразу поймал ложные имена**: зона штампа на листе А0/А1 — четверть площади, и `read_stamp` честно возвращал оттуда содержимое чертежа («икация помещений 1 этажа Наименование», «188 Холодный 186 Овощной») — причём второе на листе, где шифр и номер прочитаны ВЕРНО, то есть признак «штамп настоящий» не спасает. Добавлен фильтр по открывающему слову названия листа (план/схема/разрез/ведомость/спецификация/таблица…), общий по ГОСТ и к разделу не привязанный: выдуманное название хуже честного «не прочитано» (Г.11/Г.10). **(3) Прогон «как инспектор» через HTTP на реальном комплекте вскрыл настоящий дефект**: без ключа обе кнопки возвращали ТОЛЬКО ошибку, хотя состав комплекта — детерминированная работа, модели не требующая. Состав перенесён ДО проверки связи и сохраняется всегда; при недоступной модели прогон по-прежнему честно `error` («требования НЕ извлекались»), но инспектор получает перечень листов, таблиц и графики. Фронт показывает состав и при ошибке. **Измеренная граница**: на этом РД наименования листов не читаются вообще — ведомость чертежей тоже в кривых; чтобы их получить, нужен просмотр изображения, это следующий шаг, а не забытый. 446 тестов.

## Г.99 Наименования листов не читались из-за поворота листа и склейки граф
Три причины одного пробела, найденные замером на реальных томах. **(1)** Лист А1 хранится повёрнутым (`/Rotate 270`), зона штампа считалась от визуального листа, а координаты текста лежат в другой системе — `get_text` возвращал пусто, и лист выглядел как «штамп в кривых»: 12 повёрнутых чертежей, наименование прочитано у 0. Система координат теперь определяется по факту (в каком варианте зоны есть текст), а не по соглашению — реальный CAD-экспорт и синтетика ведут себя по-разному. **(2)** Наименование бралось как склейка двух первых строк зоны и приносило с собой название раздела; теперь собирается по графе — строка, открывающаяся как название листа по ГОСТ, плюс её переносы, отсечённые от соседних граф геометрией. **(3)** Признака «похоже на название» мало: в зоне штампа РД нашёлся заголовок таблицы, НАРИСОВАННОЙ на листе. Гейт — служебные графы ГОСТ («Стадия», «Листов») текстом. Замер: на томе ПД наименований 2 → 25 (шифры схем, спецификация, таблицы), одно ложное убрано, потерь нет. На РД наименования не читаются вообще (штамп и ведомость чертежей в кривых) — для них добавлен платный шаг `name_unread_sheets`: вызов на лист, жёсткий бюджет, по умолчанию 0 (`SHEET_NAME_VISION_BUDGET`), в отчёте видно «не запускалось» против «смотрели, не разобрали». Состав тома теперь печатает и CLI, не только сервер. 451 тест.

## Г.100 Окно разбора с инспектором: замечание — запись датасета, не «обучение»
Предложение пользователя: окно общения с моделью в разделе проверки, чтобы инспектор указывал на недочёты «и параллельно её дообучал». Дообучения весов нет и не будет (Г.75), и интерфейс это говорит прямо. Реально работает другое: замечание копится как размеченная запись и подставляется в промпт примером — механизм `known_violations` (Г.37). Три решения, каждое обязательное. **(1)** Вопрос и замечание — разные сущности, разделённые НА ВХОДЕ (`kind` пуст или род ошибки): в датасет идут только замечания, а отделить их от переписки задним числом нельзя. **(2)** Замечание сохраняется ДО обращения к модели: инспектор, разобравший том без ключа, не теряет свою работу; отсутствие ответа названо словами, а не пустотой (Г.10/Г.98). **(3)** Гейт Г.11/Г.12 двойной: пример работает только после отдельного решения человека (`approved`, по умолчанию нет) И только на ДРУГИХ объектах (`exclude_documents` по именам томов прогона) — замечание с проверяемого объекта в его же промпте это не обобщение практики, а подсказка ответа самому себе. `review_dialog.py`, `POST/GET /compliance-runs/{id}/messages`, `PATCH /review-messages/{id}`, панель в карточке сверки. 12 тестов.

## Г.101 Параметр связывается с нормативом ИЗ ПЕРЕЧНЯ ЭТОГО ТОМА
Указание пользователя: параметры (расчёты, параметры воздуха) связывать с нормой и говорить в выводе, какому НД соответствует; в документах есть список норм, на основе которых выполнен проект. Источник нормативов — только перечень самого тома: модель выбирает из закрытого списка либо честно отвечает «не могу», а **пункты внутри нормативов не извлекаются вообще** — пункт, названный по памяти, выглядит как точная ссылка и потому опаснее её отсутствия. Две ступени, вторая работает только над нерешённым первой: обозначение стоит прямо в требовании (подстрока, без модели, ошибиться не может) → сопоставление по смыслу (гипотеза, помечена отдельно). Замер на реальном томе: перечень найден на листе 76, 21 норматив; различитель «перечень против оглавления» — число обозначений на листе (содержание 0, перечень 25). **Три дефекта, найденных замером, а не предположенных**: заменённое обозначение в скобках сразу после основного заводилось отдельным нормативом и забирало наименование себе — стало псевдонимом; обозначение внутри кавычек наименования тоже считалось документом; захваченная точка в конце фразы не сходилась с перечнем, и норматив, который в перечне ЕСТЬ, попадал в список отсутствующих (класс Г.68). **Побочная находка на реальных данных**: 4 норматива названы в тексте, но в перечень тома не внесены. Уточнение пользователя по ходу: «если в тексте есть ссылка на норму, которой нет в списке, это не ошибка, просто надо это учитывать» — и оно вскрыло недоиспользование: такие требования оставались БЕЗ привязки вовсе, хотя норматив назван проектировщиком прямым текстом. Теперь ссылка сохраняется с пометкой «в перечне тома нет», прямых привязок стало 10 вместо 5, а строка отчёта переформулирована с «проверить полноту» на «это НЕ нарушение, учитывать при проверке». `norms_registry.py`, `Requirement.norm/norm_source`, шаг 4 CLI, аддитивная миграция хранилища (датасет append-only, пересоздавать нельзя). 12 тестов, 475 всего.

## Г.102 Правило «код общий» переехало в жёсткие правила и получило машинную проверку
Третье указание пользователя об одном и том же: «ты опять в коде конкретно делаешь под примеры, переделай свою инструкцию, чтобы ты не увлекался». Г.88 и Г.89 уже требовали этого — и оба раза правило держалось на памяти автора, оба раза сползало, оба раза находил пользователь. Значит чинить надо не строчки, а способ соблюдения. Сделано: **правило вынесено в раздел 0 жёстких правил** (их читают всегда, приложение Г — выборочно) и **закрыто тестом** `test_code_is_generic.py`, который падает на коммите. Проверка ищет в `app/` и `scripts/` реквизиты объекта (шифр, организация, маркировка тома) и обозначения нормативов в механике; законных исключения два и оба объявлены явно — файлы-реестры наблюдений и стандарты формы документации (СПДС), применимые к любому разделу по определению. Обезличенный шаблон формы («XXX/000000/1-РД-<марка>») не считается реквизитом — Г.89 сам его и предписал. **Первый прогон нашёл 48 нарушений в 8 файлах**, из них не только комментарии: имя организации проверяемого объекта, имена его томов и — отдельно — `benchmark_stamp_ocr.py`, где эталон конкретного комплекта лежал прямо в репозитории, то есть готовый ответ для следующей «слепой» сессии (Г.24) и данные объекта в git (Г.12). Эталон вынесен в файл-аргумент, и бенчмарк заодно стал работать на любом комплекте. У самой проверки есть тест на неё: подставить строку каждого запрещённого вида и убедиться, что ловится, а разрешённые — нет (правило без проверки — это ещё одно правило на память). 480 тестов.

## Г.103 Перечень нормативов ищет модель, регулярка — запасной путь
Замечание пользователя ровно про заточку под пример: «наверное, проще, чтобы ЛЛМ определяла этот момент, мы же всё равно туда отправляем документ для выжимки». Г.101 искал раздел с перечнем по закрытому списку формулировок заголовка — а называется он у каждого проектировщика по-своему («перечень использованных нормативных документов», «нормативные ссылки», «перечень использованной литературы» и далее), и предугадать все варианты нельзя: это была заточка под уже виденные документы. Исправлено: перечень возвращает модель **тем же вызовом, которым идёт выжимка** — отдельного прохода по документу не нужно, поле `norms` добавлено в контракт ответа `requirement_llm_extract`, передаётся обратным вызовом `on_norms` (у функции один результат — требования, менять её контракт ради побочного продукта неверно). Ответ модели — недоверенные данные: обозначение принимается, только если похоже на обозначение норматива, иначе модель дописала бы в перечень «то, что должно быть в таком проекте». Поиск по тексту остался запасным путём без ключа и перекрёстной проверкой (Г.92: детерминированный путь ценен не только как запасной).

## Г.104 Обобщена сама инструкция, проверка расширена на неё
Указание пользователя сразу после Г.102: «перепиши инструкцию так, чтобы не было никакой конкретики к определённому проекту». Г.102 закрыл машинной проверкой код — и на этом остановился, хотя инструкция опаснее: код можно прочитать и понять, а инструкция задаёт способ думать до того, как открыт первый файл. Замер по CLAUDE.md: 20 строк с конкретикой, все в Приложении Г — примеры одного раздела в формулировках общих правил, наименование организации объекта, имена и маркировки его томов, обозначения нормативов из него, и раздело-специфичный пример прямо в контракте находки (раздел 5.2). `CONTEXT.md` и `GIGACODE.md` оказались чисты. Каждая формулировка переписана через РОЛЬ: вместо отраслевого имени расчётной таблицы — «расчётная таблица ПД, назначающая помещению сущность»; вместо конкретной отметки с пределом огнестойкости — описание их соседства на листе; в контракте находки конкретный класс материала заменён угловыми скобками. **Различие, которое чуть не потерялось**: явная пометка границы («это раздело-специфично», «статус n=1») — НЕ конкретика, а признание области применимости, и убирать её нельзя: это выдало бы узкий механизм за общий, то есть настоящий overfitting по Г.42. Различие внесено в само правило раздела 0. **Разделение по назначению файлов**: инструкция обязана быть общей, журнал доказательств — конкретен по существу («на таком-то томе измерено то-то»), иначе доказательство превращается в лозунг; реквизиты объекта запрещены и там, потому что доказательности не добавляют, но это данные объекта в git (Г.12) и готовый ответ следующей «слепой» сессии (Г.24) — из журнала вычищены 9 таких вхождений. Проверка `test_code_is_generic.py` расширена двумя тестами: инструкционные файлы — без реквизитов, нормативов и лексики одного раздела; журнал — без реквизитов. 484 теста.

## Г.105 Знание о разделах — накапливаемая база, а не константа в коде
Указание пользователя: «код должен быть обобщённым, а конкретика и примеры по проектам — уже в ходе эксплуатации; она будет пополняться, чтобы быстрее работала и находила недочёты. Должна быть база данных с типизацией разделов, а исполняемость программы — общей, чтобы она не выискивала конкретный раздел из примеров, а просто шла по порядку по каждому тому независимо и там определяла, какой это том». Аудит поведения перед работой: гейтов по разделу в логике не осталось ни одного (Г.88 держится), программа действительно идёт по томам одинаково. Не хватало другого — накопления. `section_profile.py`: механизм БЕЗ единого знания о каком-либо разделе, умеющий три вещи — запомнить наблюдение, посчитать, в скольких РАЗНЫХ томах оно встречалось, и отдать накопленное подсказкой в промпт и строкой в отчёт. Наблюдения трёх видов и все три — побочный продукт уже сделанной работы, без единого дополнительного вызова модели: устойчивые обороты требований (соседние значимые слова, повторяющиеся в томе от 3 раз; никакой морфологии и никаких словарей — метод одинаков для раздела, которого никто не видел), нормативы из перечня тома, реально встреченные типы таблиц. **Единица счёта — ТОМ, а не вхождение**: оборот, встреченный сто раз в одном томе, это по-прежнему n=1, привычка одного автора; повторный разбор того же файла счётчик не увеличивает, иначе «уверенность» росла бы от перезапусков. **Подсказка начинает работать со ВТОРОГО тома раздела** (`HINT_MIN_DOCUMENTS=2`) и говорит, что не является списком того, что обязано найтись — факт по-прежнему обязан быть на странице (раздел 0, п.7). **Имя тома не хранится**, только отпечаток — для идемпотентности его достаточно, а имя это реквизит объекта (Г.12). Промпт извлечения теперь собирается на КАЖДУЮ пачку по разделу этой пачки: в одном прогоне бывают тома разных разделов, и общий промпт означал бы подсказку от чужого раздела. **Проверено на реальном комплекте**: три тома подряд, профиль растёт 1→2→3, тип таблицы дошёл до n=3; ни один оборот пока не подтверждён двумя томами — честный результат, тома слишком разные по природе. **Баг, найденный собственным тестом**: повтор значения внутри одного вызова пытался завести отметку тома дважды и падал на ограничении уникальности — вызов теперь схлопывает повторы сам. 496 тестов.

## Г.106 Требование адресует помещения ТИПОМ — подсказка по названию подключена и сужена
Сверка слепого прогона с реальным перечнем нарушений: два случая из трёх ПД адресует не номером, а типом помещения («в таких-то по назначению»), номера стоят в экспликации того же тома — `rooms` пуст, листов для просмотра подобрать не по чему. Механизм сопоставления по названию (Г.57) существовал и **не вызывался никем** (класс Г.62); подключён как отдельное поле `rooms_by_name` — номер в скобках это факт документа, совпадение по названию — догадка, и в отчёте они помечены по-разному. **Замер: требований с листами для просмотра 64 → 128, все три случая эталона теперь выходят с листами.** Два дефекта, найденных тем же замером: (1) сверка печатала «подходящих листов не найдено», когда листы были подобраны, а зрение просто не запускалось, — НЕвыполненный шаг выдавался за отрицательный результат поиска (Г.10), теперь отдельное состояние; (2) слово, которым назван предмет тома, стоит почти в каждом его требовании и цепляло одно помещение к 27 требованиям — такие слова вычисляются из требований самого прогона (`topic_prefixes`, порог 5%: замер дал 7,1% у слов предмета против 0,3-0,6% у различающих, граница между порядками), список нигде не зашит и у каждого раздела свой; подсказок 105 → 69, все три случая эталона сохранены. Обратная, room-side, граница НЕ введена: на реальных данных родовое название помещения (8,3% названий) неотделимо порогом от осмысленного (11,7%) — правила без наблюдения не заводим (Г.11). 501 тест.

## Г.107 Акцент на определённые тома был не только в коде, но и в порядке работы
Указание пользователя: «принцип един для всех, не должно в коде и порядке работы быть акцента на определённые тома». Проверено машиной, а не глазами, и потому нашлось четыре класса сразу. **(1) Порядок работы нарушал правило сильнее кода**: какой файл считать проектной документацией, а какой рабочей, решал человек перечислением файлов — и ровно так из прогона молча выпал самый толстый том комплекта (568 текстовых страниц из 696, то есть четыре пятых рабочей документации). Теперь стадию определяет программа по графе шифра основной надписи (`document_stage`, ГОСТ Р 21.101), а разбитый на файлы том узнаётся по общей шифровой части имени и наследует стадию соседа с явной пометкой «взято у другого файла». Прогон стал брать каталог, а не список файлов. **(2) Реквизиты объекта в коде**: имена файлов томов, шифр комплекта, кодовое имя объекта и имя набора — 20 вхождений в 9 файлах, все в комментариях, где их никто не ищет. Проверка `test_code_is_generic` расширена тремя шаблонами и ловит этот класс; заглушка-роль («ПД.pdf», «путь/том.pdf») от реквизита отличается по форме имени, а не по списку. **(3) Лексика одного раздела в механике**: проверка Г.102 искала её ТОЛЬКО в инструкции, на код правило не распространялось вовсе — 8 файлов с отраслевыми словами, из них 4 говорили словами ровно одной дисциплины, включая промпт извлечения требований, то есть акцент доезжал до модели. Новый тест считает дисциплины по группам слов и виноватым признаёт файл, где ВСЕ слова из одной: перечисление нескольких дисциплин — не заточка, а способ показать, что наблюдение общее. **(4) Словарь в механике**: родовое слово в названиях помещений (Г.57) переехало в `data/generic_room_words.json` — вычислить его из данных прогона не удалось, и это измерено (8,3% названий против 5,9% и 11,7% у осмысленных; 5 разных слов-соседей против 3 — границы нет ни по одному признаку), а подгонять порог под один пример нельзя (Г.11). **Прогон после правок**: 4 тома, стадия определена программой у всех (3 из штампа, 1 наследованием), требований ПД 308, требований с листами для просмотра 129, все три случая эталона выходят с листами. Честно не выполнено: смысловая сверка текста РД и извлечение требований на 97 пачках из 112 — роль модели в этой сессии играю я, бюджет ограничен; и то и другое названо в отчёте отдельной строкой, а не выдано за пустой результат (Г.10). 504 теста.

## Г.108 Правило «не подгонять под примеры» стало процедурой с машинной проверкой
Десятая правка пользователя об одном и том же: «если бы я тебе сейчас 100 томов кинул, ты бы чисто под них всё писал?». Ответ по факту был «отчасти да», и впервые он измерен: в механике **53 числовые константы уровня модуля**, из них 18 — пороги, выведенные из одного-двух реальных томов. На сотне томов такой порог ошибается там, где его никто не мерил, и подстроить его может только автор кода — то есть программа не улучшается от эксплуатации, что и есть суть претензии. **Сделано три вещи.** (1) **Реестр констант с проверкой** (`test_constants_are_declared.py`, включён в `make check-generic`): каждая числовая константа объявлена одним из видов — БЮДЖЕТ, ПРАВИЛО ЯЗЫКА, ГЕОМЕТРИЯ/ФОРМАТ, ПОДОГНАНО с честным `n=`; незаявленная константа роняет проверку. Подгонка не запрещена — запрещена НЕЗАМЕТНАЯ подгонка, потому что именно незаметность стоила десяти заходов. (2) **Порядок любой правки** внесён в раздел 0 п.8 шестью пунктами: откуда берётся число, где законное место наблюдения, «не отсеивай там, где можно ранжировать», проверка формулировкой без единого слова о текущем объекте, отрицательный результат измерения как результат, и контрольный вопрос про сто томов. (3) **Подогнанный порог Г.106 убран вовсе.** Отделить слово предмета тома от слова, называющего место, пробовали четырьмя способами (доля требований, доля названий помещений, число разных слов-соседей, двусторонний idf) — **ни один не дал границы, которая не была бы подгонкой**, и это записано как отрицательный результат, а не спрятано за числом «примерно посередине». Вместо отсева подсказка теперь РАНЖИРУЕТ: вес слова считается по данным (редкость среди названий помещений документа, делённая на число РАЗНЫХ томов раздела, где слово уже встречалось), в формуле нет ни одной константы, а сколько взять из порядка — решает бюджет листов. Накопительная база получила четвёртый вид наблюдения (значимые слова требований, счёт по томам), поэтому различение уточняется само по мере эксплуатации. **Прогон**: 4 тома, стадия у всех определена программой, требований ПД 308, с листами для просмотра 165, все три случая эталона выходят с листами. **Честная граница**: накопленные 4 тома — это один объект, а тома одного объекта повторяют лексику названий его помещений, поэтому счётчик томов пока различает слабо; он начнёт работать на томах РАЗНЫХ объектов, то есть ровно в том сценарии, о котором спрашивал пользователь. 509 тестов.

## Г.109 Оригиналы — отдельная база; лимиты, срок хранения, разрезание тяжёлых томов
Вопрос пользователя «куда девается загруженный файл» вскрыл, что на него честного ответа не было. Замер: **2268 файлов, 4,93 ГБ** в каталоге загрузок; уникального содержимого — **643 файла, 60 МБ**, то есть **4,87 ГБ занимали дубликаты** одних и тех же томов, залитых заново при каждом запуске. Ни срока хранения, ни дедупликации, ни лимитов на размер и число страниц не было вовсе — три незакрытых требования Б.4/Б.5 сразу. **Сделано.** (1) `file_store.py` — отдельная база оригиналов (`data/file_store.db`), ключ записи и есть отпечаток содержимого, поэтому дедупликация не оптимизация, а свойство устройства; каталог загрузок стал восстановимым КЭШЕМ, а не источником истины (удалили — восстановится). Отдельная база, а не общая с разбором: оригинал велик, читается редко и обязан уметь исчезнуть по сроку, не задев результат разбора, который мал и живёт дольше. (2) Загрузка: тип по сигнатуре, а не по расширению; лимиты на размер и число страниц; срок хранения; уборка не трогает оригинал, на который ссылается хоть один документ, — сколько бы ни стоял срок. (3) `document_split.py` — тяжёлый том режется на части ПО ВЕСУ, а не по числу страниц (страница прозы и полноформатный чертёж отличаются на порядки), с сохранением ИСХОДНОЙ нумерации листов: часть несёт смещение, лист N остаётся листом N, иначе ссылка «смотреть лист N» перестаёт работать. Тест требует, чтобы части в сумме давали документ страница в страницу. (4) Перенос накопленного (`scripts/migrate_uploads.py`) — сначала считает и докладывает, удаляет только с `--remove`; после переноса каталог 5,8 ГБ → 16 МБ, диск освободился на 6 ГБ. **Реальный баг, найденный самим переносом**: два процесса, укладывающих одно содержимое одновременно, роняли запись на ограничении уникальности. Для хранилища по содержимому это не ошибка — ключ и есть отпечаток, значит запись уже та же; `put` стал идемпотентным, тест воспроизводит именно гонку, а не два вызова подряд. 531 тест.

## Г.110 Сертификат добавляется файлом в каталог, а не правкой переменных
Вопрос пользователя: «как добавить SSL с ПК, может файл закинуть?». Ответ по факту был «пропишите путь в переменной окружения», а первый совет из интернета на `CERTIFICATE_VERIFY_FAILED` — отключить проверку, что снимает защиту от подмены на всём канале и ничего не чинит. Сделано так, как пользователь и предположил: файл `.pem/.crt/.cer` кладётся в каталог `certs/`, дальше ничего. Каталог просматривается сам, найденное **склеивается с системным набором** (`certifi`), поэтому починка одного хоста не ломает проверку остальных; собранный набор кэшируется под именем-отпечатком, поэтому замена файла подхватывается, а лишней сборки на каждом вызове нет. Переменная окружения остаётся и побеждает каталог. **Состояние TLS показывается ВСЕГДА** — в ответе проверки связи и строкой в интерфейсе: «системный набор» / «набор из certs/» / красным «проверка ОТКЛЮЧЕНА»; раньше отключённая проверка выглядела ровно так же, как исправная работа. На ошибке сертификата сообщение называет действие, а не только диагноз. **Баг, найденный собственным тестом**: первая версия клала собранный набор рядом с сертификатами, он попадал в собственный список, отпечаток менялся на каждом вызове — набор пересобирался бесконечно; вынесен в подкаталог. 539 тестов.

## Г.110-бис Форматы сертификата: двоичный и архив — иначе набор молча не читается
Пользователь прислал официальную выгрузку удостоверяющего центра. Она оказалась **архивом с файлами `.cer` в двоичном виде (DER)** — ровно тем, что механизм Г.110 принять не мог: он склеивал байты как есть, и OpenSSL такой набор не читает. Отказ был бы молчаливым: набор собран, путь подставлен, проверка по-прежнему не работает. Исправлено: формат определяется по СОДЕРЖИМОМУ (а не по расширению) и двоичный преобразуется в текстовый; архив читается без распаковки, потому что именно так сертификаты и отдают. **Баг, найденный тестом на негодном файле**: перекодировка в PEM никаких проверок не делает, поэтому посторонний файл превращался в правдоподобный блок и ломал ВЕСЬ набор — один мусорный файл в каталоге отключал проверку сертификатов целиком. Теперь каждый файл проверяется тем же разбором, каким набор будут читать потом, негодный пропускается с причиной, остальные остаются. Замер на присланной выгрузке: набор собрался из 121 системного корня и 3 присланных, OpenSSL его принимает. Связь с провайдером из облачной среды по-прежнему не проходит — но теперь это чистый сетевой отказ (`Connection reset`), а не TLS: разница видна в тексте ошибки. 542 теста.

## Г.111 В браузере открывалась старая версия — её отдавал сервис-воркер
Пользователь прислал скриншот: в интерфейсе экраны, которых в коде нет с Г.85 (удалены вместе с витриной CRM), и «не удалось выполнить запрос» на каждом. Причина не в сборке и не в ветке: приложение подменял сервис-воркер, установленный прошлой сборкой. Он отдаёт свой кэш и переживает перезапуск сервера, обновление кода и обычное обновление страницы — а само по себе это не чинится: пока по адресу `/sw.js` ничего нет, браузер оставляет прежнего воркера. Сделано: в режиме разработки по этому адресу отдаётся **воркер-самоликвидатор** — чистит кэши, снимает регистрацию, перезагружает открытые вкладки; в сборке добавлены `skipWaiting`/`clientsClaim`/`cleanupOutdatedCaches`, чтобы новая версия вытесняла предыдущую сразу, а не ждала закрытия всех вкладок. **Вторая причина той же картинки**: проксирование `/api` на порт 8000 осталось от удалённого Г.85 сервера и вело в закрытый порт — отсюда «не удалось выполнить запрос» вместо понятной причины; в коде к `/api` не обращается ничего, правило убрано. Заодно сервис-воркеру запрещено перехватывать обращения к движку разбора: это данные, а не оболочка, и отдавать их из кэша значит показывать инспектору вчерашний результат как сегодняшний. **Отдельно, из того же захода**: сертификаты, распакованные проводником Windows в подпапку, не находились — поиск шёл только по самому каталогу, и молча оставался системный набор. Теперь поиск идёт вглубь; проверено на трёх раскладках (файлы в каталоге, файлы в подпапке, архив) — во всех 124 корня, из них 3 нужных. 543 теста.
