# НАДЗОР.ИИ

Модуль предиктивного строительного надзора для цифровой экосистемы стройкомплекса
Москвы. Задача города № 10, Комитет государственного строительного надзора
города Москвы.

[![Проверки качества](https://github.com/DmitryKDI/DmitryKDI/actions/workflows/ci.yml/badge.svg)](../../actions/workflows/ci.yml)
[![Проверки безопасности](https://github.com/DmitryKDI/DmitryKDI/actions/workflows/security.yml/badge.svg)](../../actions/workflows/security.yml)

## Что делает

Инспектор загружает комплекты документации и получает ответ на один вопрос:
**на что смотреть в первую очередь и почему**.

Работа идёт в три шага, каждый — отдельной кнопкой:

1. **Разбор проектной документации** — из текста извлекаются требования с
   привязкой к разделу, тому и странице. Состав комплекта (сколько листов
   чертежей, сколько текстовых, что не прочиталось) считается всегда, даже без
   связи с языковой моделью.
2. **Разбор рабочей документации** — тот же механизм, другая сторона комплекта.
3. **Сверка** — по списку требований проектной документации: что подтверждается
   текстом или чертежами рабочей, а что нужно посмотреть глазами. Совпадение
   обозначения само по себе не подтверждает требование. Текстовое подтверждение
   требует цитаты из указанного источника и оценки выполнения всего условия.

Результат — **гипотезы для проверки, а не заключение о нарушениях**. «Требует
проверки» означает «подтверждения не нашлось в доступных данных»: в рабочей
документации мало связного текста по природе, подписи чертежей переведены в
кривые. Решение принимает инспектор.

## Быстрый старт

Нужны Python 3.11+ и Node.js 22.

```bash
make install          # виртуальное окружение и зависимости
./scripts/start-all.sh    # движок на 8010 и интерфейс на 5173 одной командой
```

Интерфейс — http://localhost:5173, описание программного интерфейса —
http://localhost:8010/docs.

Отдельными шагами:

```bash
make backend    # движок разбора на порту 8010
make web        # интерфейс на порту 5173
```

На Windows: `START-NADZOR.bat` (работает через WSL, обновляется из git сам).
Подробно — [docs/ЗАПУСК.md](docs/ЗАПУСК.md).

## Ключ языковой модели

Ключ не вводится в интерфейсе и не хранится в репозитории. Положите файл,
выданный в личном кабинете провайдера, в каталог `secrets/` — сервер найдёт его
сам при запуске. Каталог в git не идёт.

Порядок поиска: настройки в базе → переменная окружения → файл в `secrets/`.
Файл предпочтителен: он переживает и обновление программы, и пересборку базы.

Российские корневые сертификаты кладутся файлами в `certs/` (`.pem`, `.crt`,
`.cer`, `.der`, `.cert`, читается и zip) — набор собирается автоматически и
склеивается с системным.

Диагностика связи по шагам: `.venv/bin/python scripts/check_gigachat.py`.

## Что внутри

```
packages/backend/app/     движок: разбор, сверка, хранилища, LLM, HTTP API
packages/backend/tests/   582 теста
frontend/src/             интерфейс: React 18, TypeScript, Vite, PWA
scripts/                  установка, запуск, диагностика, утилиты
data/                     реестр наблюдений, файловые базы
docs/                     документация и журнал решений
certs/  secrets/          сертификаты и ключи (в git не идут)
```

Три файловые базы SQLite, разделённые осознанно: рабочее состояние прогонов,
оригиналы документов по отпечатку содержимого и память разбора страниц.
Ни PostgreSQL, ни Docker для разработки не нужны.

## Качество

```bash
make test            # 582 теста движка
make check-generic   # код не заточен под конкретный пример (обязательно перед коммитом)
make lint            # стиль
make security        # bandit и pip-audit
cd frontend && npm run build
```

Текущее состояние проверок, включая красные, — в
[HANDOFF.md](../HANDOFF.md), раздел «Результаты проверок».

## Безопасность

Основная угроза — **инъекция инструкций через проверяемый документ**: документы
предоставляет поднадзорное лицо, заинтересованное в сокрытии нарушений. Любой
текст документа, попадающий в промпт, помечен как недоверенный ввод. Подробно —
[docs/threat-model.md](docs/threat-model.md).

Ключи только в переменных окружения или в `secrets/`; `.env.example` содержит
одни имена переменных. Реквизиты проверяемых объектов в репозиторий не идут.

## Правовая модель

Система формирует **гипотезы**, а не заключения. Ни одно юридически значимое
действие не совершается автоматически. Оговорки о статусе выводов в отчётах —
часть продукта, а не вежливость: без них отчёт читается как обвинение.

## Документация

| Файл | О чём |
|---|---|
| [AGENTS.md](../AGENTS.md) | правила разработки, команды, что нельзя менять |
| [HANDOFF.md](../HANDOFF.md) | состояние: что готово, что сломано, что дальше |
| [CONTEXT.md](CONTEXT.md) | карта: что читать сейчас |
| [CLAUDE.md](CLAUDE.md) | техзадание и Приложение Г — правила механики |
| [docs/PRILOZHENIE-G-ISTORIYA.md](docs/PRILOZHENIE-G-ISTORIYA.md) | журнал решений с доказательствами |
| [docs/СТРУКТУРА-ПРОГРАММЫ.md](docs/СТРУКТУРА-ПРОГРАММЫ.md) | устройство программы простыми словами |
| [docs/ЗАПУСК.md](docs/ЗАПУСК.md) | запуск на машине инспектора |
| [docs/threat-model.md](docs/threat-model.md) | модель угроз |
| [docs/economics.md](docs/economics.md) · [docs/roadmap.md](docs/roadmap.md) · [docs/import-substitution.md](docs/import-substitution.md) | экономика, дорожная карта, импортозамещение |

## Ограничения

- Провайдер языковой модели подключается ключом; без него разбор доходит до
  состава комплекта и честно сообщает, что требования не извлекались.
- Сверка комплекта в сотни листов — десятки и сотни обращений к модели и
  десятки минут работы. Прогресс виден, прогон останавливается кнопкой.
- Прототип CRM-витрины (дашборд, журналы, аудит, моки СУДИР и ИАИС ОГД) удалён
  из репозитория по решению владельца проекта: постановка задачи осталась в
  `CLAUDE.md`, кода больше нет.
- Все организации и физические лица в демонстрационных данных вымышлены.
